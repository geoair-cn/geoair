# V4 期望

> 本文档记录**用户对 V4 的期望**，属于**需求 / 期望记录**。
>
> - 本文档**不是**可行性分析，**不是**设计文档。可行性判断、技术选型、实现方案属于另一份文档
>   （`V4脱离Spark可行性分析.md`）。截至本文撰写时，该文档**尚不存在**（已在
>   `H:\gitee\geoair-root\geoair-framework\` 下确认无此文件，`工作总结.md` 也记录其"刚启动即被中断，无产出文档"）。
> - 本文档中"必须保留的能力"三张清单（输入类型 / 输出类型 / 已实现的 tippecanoe 参数）
>   **全部逐字段核对过代码**，字段名取自源码，不是凭印象整理。
> - 若某条期望在技术上难以成立，本文档**不下判断**，只在第八节"待确认的开放问题"中提一句。

---

## 一、一句话期望

**脱离 Spark 框架，实现 tippecanoe 的全部能力，同时完整保留 V3 已有的全部输入 / 输出能力；
如果可行，新建一个 V4。**

---

## 一之二、V4 的设计目的（用户 2026-09-20 补充）

这三条是用户对"V4 该往哪儿长"的表态，后续所有取舍都以它们为准：

1. **允许计算密集，但要尽量减少内存占用。**
   与"省 CPU"相比，宁可多算几遍，也不要把数据堆在内存里。落点：有界选择（容量 N 的堆）、
   溢写换内存、流式归并 —— 削的时机只影响 CPU 与内存，不影响产物。
2. **设计目的是完整复刻 tippecanoe。**
   不是"V3 加上几个开关"，而是以 tippecanoe 的行为为准绳逐项对齐（含它没实现的部分，
   见第四节）。
3. **视角从"瓦片"提升到"全局"。**
   原话：**"Spark 是从瓦片的角度去调优，我希望 V4 是从全局的角度。"**
   Spark 的切分单位就是瓦片：一个 reduce 分组 = 一块瓦片，削减在瓦片内独立发生，
   于是"这块瓦片留多少"取决于它自己攒了多少、削了几次（实测同一份数据换一个内存参数就能差 18%）。
   V4 要反过来 —— **先定全局判据，再落到每块瓦片上**：同一层级用同一套参数、同一个身份排名，
   削多少由全局排名决定，与分块方式、溢写位置、并行度都无关。

> 第 3 条目前只落地在"聚合削减"这一步（见 `V4开发计划.md` 7.8：按 (身份排名, 输入序号, 批内下标)
> 取最小的 N 个，与到达顺序和溢写无关）。真正"全局"的下一步是第四节说的
> **zoom level 级降级决策**（先扫一遍整层、算出本层该丢多少/合并多少，再逐瓦片执行），
> 那才是 tippecanoe 的 `-ad`/`-an`/`-aN`/`-aS`/`-aG` 的实际作用域。

---

## 二、要做什么

1. **脱离 Spark**：不再以 Spark 作为切片执行的运行时框架。
2. **实现 tippecanoe 的全部能力**：不只是 V3 已经做到的这部分，还包括 V3 目前**刻意未实现**的那些
   （清单见第四节）。
3. **完整保留 V3 的全部输入 / 输出能力**：输入类型、输出类型、已落地的参数项一个都不能少
   （清单见第三节）。
4. **如果可行，新建 V4**：作为 V3 之后的新一代切片实现。

---

## 三、必须保留的能力

> 以下三张表**全部从代码核实**，路径与字段名均为源码实际存在的内容。
> 核实范围：`geoair-framework` 仓库 `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/`
> 下的 `vectile/dto/v3/`（参数定义）与 `vectile/impl/v3/`（实现），以及调用方仓库
> `geoair-mvt-server` 的 `MvtLayerSpec` / `TileParameterBuilder`。

### 3.1 输入类型（从代码核实）

V3 的图层输入类型由枚举 `V3TileInputType`（`dto/v3/V3TileInputType.java`）定义，**只有两个值**：

| 枚举值 | 含义 | 配置类 | 实际读取实现 | 关键字段（源码实际存在） |
| --- | --- | --- | --- | --- |
| `JDBC` | 通过 JDBC 查询空间数据 | `V3JdbcInputConfig` | `impl/v3/input/V3JdbcFeatureReader.java` | `dataSource`（`DataSourceConfig`）、`queryStatement`、`readStrategy`（默认 `ReadStrategy.ID_PAGE`）、`maxPartitionNum`（默认 `20`） |
| `GEOJSON` | 从 GeoJSON 文档或 GeoJSON Lines 文件读取 | `V3GeoJsonInputConfig` | `impl/v3/input/V3GeoJsonFeatureReader.java` | `paths`（`List<String>`）、`mode`（默认 `V3GeoJsonMode.AUTO`）、`minPartitionNum`（默认 `20`）、`skipInvalidFeature` |

**关于 JDBC 的数据库种类：需要区分"代码能支持"与"当前实际能配"两个层次。**

- **参数层不限制数据库**：`V3JdbcInputConfig.dataSource` 的类型是 `DataSourceConfig`，
  读取时走 `AdvExecutorFactory.getAdvExecutorByDataSource(DataSource)`，
  该工厂**按 JDBC 产品名动态分派方言**。已注册的方言提供者（`AdvExecutorFactory` 静态块）
  共 **9 个**：MySQL、MariaDB、PostgreSQL、Kingbase、OpenGauss、SQLServer、Oracle、达梦（DM）、SQLite。
  即**读取器本身能对接上述 9 类数据库**。
- **当前管理端界面只放开了 2 种**：`geoair-mvt-server` 的 `utils.js` 中
  `DbType: { POSTGRESQL: 'POSTGRESQL', MYSQL: 'MYSQL' }`，即管理端目前只能创建
  PostgreSQL 与 MySQL 两种数据源。这与用户所说的"JDBC（PostGIS / MySQL）"一致。
- 需要注意：**BBOX 读取策略依赖数据库的空间函数**。`V3DataReadUtils.buildBboxQuerySql` 拼接的是
  `public.ST_MakeEnvelope(...)` 与 `ST_Intersects(...)`，是 PostGIS 语法；
  `ID_PAGE` 策略则是纯分页 SQL，不依赖空间函数。

**GeoJSON 的格式与压缩（从代码核实）**：

- `V3GeoJsonMode` 三个值：`AUTO`（默认，按扩展名判断）、`FEATURE_COLLECTION`（标准
  FeatureCollection 或单个 Feature 文档）、`GEOJSON_LINES`（每行一个 Feature）。
- `AUTO` 的判定依据（`V3GeoJsonFeatureReader.resolveMode`）：路径以
  `.geojsonl` / `.ndjson` / `.jsonl` / `.geojsonl.gz` / `.ndjson.gz` / `.jsonl.gz` 结尾时判为
  `GEOJSON_LINES`，否则为 `FEATURE_COLLECTION`。
- **`.gz` 支持确实存在**：`GeoJsonDocumentFunction` 在文件名以 `.gz` 结尾时套 `GZIPInputStream`
  再解析。注意该分支只作用于**文档模式**（走 `binaryFiles` 的路径）；Lines 模式走 Spark `textFile`，
  由 Hadoop 的编解码器决定是否解压。
- 路径交给 Spark/Hadoop 文件系统解析，支持 `file:///`、`hdfs://`、`s3a://` 及通配符。

**另外，输入还受以下图层级参数影响（都属于要保留的能力）**：
`geomFieldName`（默认 `"geometry"`）、`idFieldName`、`sourceDataSrid`
（未设置时 JDBC 默认 3857、GeoJSON 按 RFC 7946 默认 4326，见 `resolveSourceDataSrid()`）。

### 3.2 输出类型（从代码核实）

输出类型由枚举 `V3TileOutputType`（`dto/v3/V3TileOutputType.java`）定义，**共 5 个值**，
统一由 `V3TileOutputConfig`（`dto/v3/V3TileOutputConfig.java`）承载参数：

| 枚举值 | 含义 | 写出实现 | 专属字段（源码实际存在） |
| --- | --- | --- | --- |
| `POSTGRESQL` | 写入 PostgreSQL 瓦片表（默认值） | `SparkVectorTileGeneratorV3`（`bindDelete`/`bindInsert`/`createTableIfAbsent`） | `postgresqlOutputSource`（`DataSourceConfig`，表名含 schema） |
| `LOCAL_DIRECTORY` | 写入本地/共享文件系统的 `z/x/y.pbf` 目录树 | `impl/v3/output/V3LocalTileStore.java` | `localDirectory` |
| `S3` | 写入 S3 兼容对象存储的 `z/x/y.pbf` 对象树 | `impl/v3/output/V3S3TileStore.java` | `s3Bucket`、`s3Prefix`（默认 `""`）、`s3Endpoint`、`s3Region`（默认 `us-east-1`）、`s3AccessKeyId`、`s3SecretKey` |
| `MBTILES` | 先写本地暂存目录，再归档为标准 MBTiles SQLite 文件 | `impl/v3/archive/MbtilesArchiveUtils.java` | `stagingDirectory`、`mbtilesFile`、`archiveBatchSize`（默认 `1000`） |
| `PMTILES` | 先写本地暂存目录，再归档为 PMTiles V3 单文件 | `impl/v3/archive/PmtilesUtils.java` | `stagingDirectory`、`pmtilesFile` |

**所有输出类型共用的可配项（源码实际存在）**：

| 字段 | 默认值 | 说明 |
| --- | --- | --- |
| `tileYAxis` | `TileYAxis.XYZ` | 目录名或对象 key 中使用的 Y 轴约定；TMS 时按 `grid_srid` 分别用 3857 / 4326Separate 网格换算（`getTmsY`）。**PostgreSQL 输出恒写 TMS**（`tms_y` 列）；目录/S3/归档则按 `tileYAxis` 换算 |
| `overwrite` | `true` | 是否允许覆盖已存在的瓦片文件或对象；关闭时本地/S3 均抛 `IOException`；对归档同样生效 |
| `writeMetadata` | `true` | 是否在输出根目录写入任务元数据清单（**PostgreSQL 输出不写**） |
| `metadataFileName` | `"geoair-v3-mvt.json"` | 元数据文件名或对象 key 名称 |

**瓦片路径约定**：`tilePathTemplate = "{z}/{x}/{y}.pbf"`（元数据中显式声明）。
PostgreSQL 表结构固定 10 列：`id, z, x, tms_y, y, grid_srid, tile_data, layer_name, edition, insert_time`，
并建索引 `(z,x,y,grid_srid,layer_name,edition)`。

**元数据清单内容（`buildOutputMetadata`）**：`format`（`pbf`）、`contentEncoding`
（`gzip` 或 `none`，随 gzip 开关变化）、`tilePathTemplate`、`yAxis`、`gridSrid`、`tileSetName`、
`edition`、`minZoom`、`maxZoom`、`layers`（每个图层含 `name`/`minZoom`/`maxZoom`）、
`name`、`version`、`type`（`overlay`）、`vector_layers`（含 `id`/`minzoom`/`maxzoom`/`fields`）。

**gzip 开关（`gzipPbf`，任务级，默认 `true`）覆盖以下三个位置**：
1. 本地目录 / S3 写出的瓦片数据本身（S3 还会设置 `Content-Encoding: gzip`）；
2. 元数据清单里的 `contentEncoding` 字段；
3. PMTiles 归档时 Header 的压缩标志位（`PmtilesUtils` 的 `GZIP_COMPRESSION` / `NO_COMPRESSION`）
   与瓦片数据段。MBTiles 归档走 SQLite，瓦片数据按已 gzip 的字节原样写入。

**归档的网格限制**：MBTiles / PMTiles 输出**仅支持 EPSG:3857 网格**，
`archiveTiles` 在 `outGridSrid != 3857` 时直接抛
`IllegalArgumentException("V3 MBTiles/PMTiles 输出仅支持 WebMercator（EPSG:3857）网格")`。

**任务级其它必须保留的字段（`MultiLayerTileSliceParameter`）**：
`outputConfig`、`tileSetName`、`edition`（默认 `""`）、`outGridSrid`（默认 `3857`）、
`minZoom`（默认 `4`）、`maxZoom`（默认 `15`）、`tileSizeLimitEnabled`（默认 `true`）、
`tileSizeLimit`（默认 `"2MB"`，经 `getTileSizeLimitByte()` 转换）、`gzipPbf`（默认 `true`）、
`reducePartitionNum`（默认 `3000`）、`trackId`、`layers`。
另有 ticket 编解码能力：`toBase32()` / `fromBase32(String)`（Base32 + URL 编码包装的 JSON）。

### 3.3 已实现的 tippecanoe 风格参数（逐项列出，源码实际存在的字段）

**A. 图层级——`MvtLayerSliceParameter`（共 20 个字段，其中 7 个为本轮新增的 tippecanoe 控制项）**

| # | 字段名 | 类型 / 默认值 | 对应 tippecanoe 参数 |
| --- | --- | --- | --- |
| 1 | `layerName` | `String` | — （MVT 内部图层名） |
| 2 | `inputConfig` | `V3LayerInputConfig` | — |
| 3 | `geomFieldName` | `String`，`"geometry"` | — |
| 4 | `idFieldName` | `String` | — |
| 5 | `sourceDataSrid` | `Integer`，null 时 JDBC 取 3857 / GeoJSON 取 4326 | — |
| 6 | `minZoom` | `Integer`，null 时继承任务级 | `-Z/--minimum-zoom`（图层级） |
| 7 | `maxZoom` | `Integer`，null 时继承任务级 | `-z/--maximum-zoom`（图层级） |
| 8 | `includeFields` | `List<String>`，空列表 | `-y/--include` |
| 9 | `excludeFields` | `List<String>`，空列表，**优先级高于 `includeFields`** | `-x/--exclude` |
| 10 | `sysIncludeFields` | `Set<String>`，空集合 | — （始终保留的系统字段） |
| 11 | `featureLimitEnabled` | `boolean`，`false` | `-f/--feature-limit` 的开关 |
| 12 | `featureLimit` | `Integer`，null | `-f/--feature-limit` |
| 13 | `hardFeatureLimit` | `Integer`，`8000` | — （Spark 聚合阶段的强制安全上限，非 tippecanoe 参数） |
| 14 | `coalesceDensestAsNeeded` | `boolean`，`true` | `--coalesce-densest-as-needed` |
| 15 | `dropDensestAsNeeded` | `boolean`，`true` | `--drop-densest-as-needed` |
| 16 | `simplificationLevel` | `Integer`，null 表示不额外简化 | `-D/--simplification` |
| 17 | `simplifyLines` | `boolean`，`true` | `-ps/--no-line-simplification` 的**反向**开关 |
| 18 | `preserveSharedNodes` | `boolean`，`true` | `-pS/--no-simplification-of-shared-nodes` 的**反向**开关 |
| 19 | `coalesceDistance` | `Integer`，null（屏幕单位，瓦片边长按 4096 计） | `-K/--cluster-distance` |
| 20 | `dropSmallestAsNeeded` | `boolean`，`false` | `--drop-smallest-as-needed` |
| 21 | `generateIds` | `boolean`，`false` | `-ai/--generate-ids` |
| 22 | `priority` | `int`，`0` | — （PBF 超限时的保留优先级，数值越大越优先保留） |
| 23 | `geometryMode` | `MvtLayerGeometryMode`，`ORIGINAL` | — （`ORIGINAL` / `CENTROID` / `BOUNDARY`） |

> **订正**：第 19 行原先写作 `` `-g/--cluster-distance` ``，是错的。tippecanoe 里
> **`-g` 是 gamma**（低层级点丢弃率），**`--cluster-distance` 是 `-K`**，两者是不同的参数。
> 本表的 `coalesceDistance` 是"固定聚类距离"，对应 `-K`。

**B. 任务级——`MultiLayerTileSliceParameter`（共 16 个字段，其中 2 个为本轮新增）**

| # | 字段名 | 类型 / 默认值 | 对应 tippecanoe 参数 |
| --- | --- | --- | --- |
| 1 | `outputConfig` | `V3TileOutputConfig` | — |
| 2 | `tileSetName` | `String` | — |
| 3 | `edition` | `String`，`""` | — |
| 4 | `outGridSrid` | `int`，`3857` | — |
| 5 | `minZoom` | `int`，`4` | `-Z/--minimum-zoom` |
| 6 | `maxZoom` | `int`，`15` | `-z/--maximum-zoom` |
| 7 | `tileSizeLimitEnabled` | `boolean`，`true` | `--no-tile-size-limit` 的**反向**开关 |
| 8 | `tileSizeLimit` | `String`，`"2MB"` | `-M/--maximum-tile-bytes` |
| 9 | `buffer` | `Integer`，`8`（瓦片内部坐标，边长为 4096） | `-b/--buffer` |
| 10 | `tileSizeOptimizeRounds` | `int`，`4` | — （超限时的最大优化轮次，非 tippecanoe 参数） |
| 11 | `gzipPbf` | `boolean`，`true` | — （PBF gzip 压缩开关） |
| 12 | `reducePartitionNum` | `int`，`3000` | — |
| 13 | `trackId` | `String`，UUID | — |
| 14 | `layers` | `List<MvtLayerSliceParameter>` | — |

**C. 管理端（APL）侧对应字段——`MvtLayerSpec`（共 18 个字段）**

`MvtLayerSpec`（`geoair-mvt-server` 的 `core/dto/MvtLayerSpec.java`）是 UI 与 V3 参数之间的桥，
由 `TileParameterBuilder.buildLayer(...)` 逐项翻译为 `MvtLayerSliceParameter`：

`layerName`、`dataSetId`、`dataSetName`、`minZoom`、`maxZoom`、`includeFields`、`excludeFields`、
`featureLimitEnabled`、`featureLimit`、`hardFeatureLimit`、`coalesceDensestAsNeeded`、
`dropDensestAsNeeded`、`simplificationLevel`、`simplifyLines`、`preserveSharedNodes`、
`coalesceDistance`、`dropSmallestAsNeeded`、`generateIds`、`priority`、`geometryMode`。

**D. 输出配置字段——`V3TileOutputConfig`（共 17 个字段）**

`outputType`、`postgresqlOutputSource`、`localDirectory`、`stagingDirectory`、`mbtilesFile`、
`pmtilesFile`、`archiveBatchSize`、`s3Bucket`、`s3Prefix`、`s3Endpoint`、`s3Region`、
`s3AccessKeyId`、`s3SecretKey`、`tileYAxis`、`overwrite`、`writeMetadata`、`metadataFileName`。

**E. 输入配置字段**

- `V3LayerInputConfig`：`inputType`、`jdbc`、`geoJson`（3 个字段）
- `V3JdbcInputConfig`：`dataSource`、`queryStatement`、`readStrategy`、`maxPartitionNum`（4 个字段）
- `V3GeoJsonInputConfig`：`paths`、`mode`、`minPartitionNum`、`skipInvalidFeature`（4 个字段）

---

## 四、要补齐的 tippecanoe 能力

`V3切片改动说明.md` 明确记录了**本轮刻意未实现**的 tippecanoe 参数。文档标题为
"本轮未实现的 tippecanoe 参数"，其正文如下（**完整引用，以文档实际内容为准**）：

> **本轮未实现的 tippecanoe 参数**（V3 当前不支持，属于后续可补齐项）：
>
> - `--extend-zooms-if-still-dropping`：即使已到最大级别，若仍在丢要素则继续扩展级别
> - `--detect-shared-borders`：检测并简化相邻多边形的共享边界
> - `--reorder`：按要素在瓦片内的空间关系重排要素顺序
> - `--hilbert`：按 Hilbert 曲线顺序排列要素
> - `--accumulate-attribute`：对指定属性做累加合并
> - `--attribute-type`：强制指定属性类型
> - `--preserve-input-order`：保持输入要素顺序
> - `-B/--base-zoom`：指定基础层级并据此决定要素保留策略
> - `--minimum-detail`：保留要素的最小细节层级
> - `--no-tiny-polygon-reduction`：关闭微小多边形合并
> - `-T/--no-tile-stats`：不写出瓦片统计信息

**用户期望 V4 把这 11 项全部补上**，使 V4 具备 tippecanoe 的完整能力，而不只是 V3 已达成的部分。

> 说明：上表已按 `V3切片改动说明.md` 的实际内容逐条照录（11 项）。
> 若该文档后续修订、条目有增减，应以文档最新内容为准。

### 另有一批降级类参数同样缺失（核对补充）

上面 11 项取自 `V3切片改动说明.md`。为了弄清"V3 到底实现了 tippecanoe 的哪些降级手段"，
另外按 tippecanoe 官方 README 的降级类参数清单逐项对源码做了 grep 核实，
发现**另有 5 项同样没有实现，且不在上面那份清单里**：

| tippecanoe 参数 | 官方语义（README 原文要点） | 源码核实结果 |
| --- | --- | --- |
| `-ad` `--drop-fraction-as-needed` | 每个 zoom level 动态丢弃一定比例的要素 | `dropFractionAsNeeded`：**0 个文件** |
| `-aN` `--coalesce-smallest-as-needed` | 把最小要素合并进邻近要素（主要给面用，**对点无效**） | `coalesceSmallestAsNeeded`：**0 个文件** |
| `-aS` `--coalesce-fraction-as-needed` | 每个 zoom level 动态把一定比例的要素合并到邻近要素 | `coalesceFractionAsNeeded`：**0 个文件** |
| `-aC` `--cluster-densest-as-needed` | 瓦片过大时**动态发现**合适的聚类距离 | `clusterDensestAsNeeded`：**0 个文件**（只有固定距离版 `-K`，见下表） |
| `-aG` `--increase-gamma-as-needed` | 瓦片过大时增大 `-g` gamma 来压体积 | `increaseGammaAsNeeded`：**0 个文件**。源码里唯一的 `gamma` 是 `GirImageUtil.gammaCorrection`（图像伽马校正），与点密度无关 |

**已实现的那几项（同样 grep 核实，供对照）**：

| tippecanoe 参数 | V3 里的对应实现 | 状态 |
| --- | --- | --- |
| `-as` `--drop-densest-as-needed` | `dropDensestAsNeeded` + `V3FeatureUtils.filterBySpatialDensity` | 有 |
| `-aD` `--coalesce-densest-as-needed` | `coalesceDensestAsNeeded` + `V3FeatureUtils.coalesceBySpatialDensity` | 有 |
| `-an` `--drop-smallest-as-needed` | `dropSmallestAsNeeded` + `MultiLayerMvtEncoderV3.sortedByScreenSize` | 有（作用域不同，见下一节） |
| `-K` `--cluster-distance` | `coalesceDistance` + `MultiLayerMvtEncoderV3.clusterPoints` | 有 |
| 逐级降低 detail | `simplificationLevel` + `encodeAll` 的 `extraSimplify` 逐级提升 | 有 |
| `-pd` `--force-feature-limit` | `featureLimitEnabled` + `featureLimit` + `applyFeatureLimits` | **近似**：V3 是"削到上限、且带下限保护"，不是 tippecanoe 的"按比例丢弃" |

也就是说，V4 在降级类参数上要补的是 **5 项新增 + 1 项语义对齐**（`-pd`）。

### 作用域差异：tippecanoe 的降级多数是 zoom level 级，V3 都是瓦片级

这是核对中发现的、比"少几个开关"更实质的一处差距。

tippecanoe 的 `-ad` / `-an` / `-aN` / `-aS` / `-aG` 都是**整个 zoom level 级**的决策
（README 原文：*applies to the entire zoom level, not to each tile*）——
同一层级内跨瓦片统一决定"丢多少 / 合并多少 / gamma 取多少"，
因此**相邻瓦片的削法一致，瓦片边界不会出现密度突变**。

V3 的实现全部是**瓦片内独立决策**（`MultiLayerMvtEncoderV3.encodeWithTileLimit`
以单块瓦片为单位削减）。tippecanoe 文档专门批评过瓦片级的做法：

> `-pd` or `--force-feature-limit`: ... It will probably look ugly at the tile boundaries.
> You probably don't want to use this.

**因此"补齐这几个参数"不等于"多几个开关"**：若 V4 要真正对齐 tippecanoe 的这组能力，
需要引入一个 **zoom level 级的决策层**（先扫一遍整层、算出本层该丢多少 / 合并多少，
再逐瓦片执行）。这属于架构层面的变化，宜在可行性分析里单独评估（另见第八节）。

### 默认限额差异：tippecanoe 是 500K，V3 是 2MB

核对参数时顺带发现的一处默认值差异，不是缺陷，但会影响"对齐"的判定口径。

tippecanoe 的默认限额（README 原文）：

| 选项 | 默认值 | 说明 |
| --- | --- | --- |
| `-M bytes` / `--maximum-tile-bytes=bytes` | **500K**（500,000 字节） | *Use the specified number of bytes as the maximum compressed tile size instead of 500K* |
| `-O features` / `--maximum-tile-features=features` | **200,000** | *Use the specified number of features as the maximum in a tile instead of 200,000* |
| `-pk` / `--no-tile-size-limit` | 关闭字节限制 | |
| `-pf` / `--no-feature-limit` | 关闭要素数限制 | |

V3 的对应默认值：

| V3 参数 | 默认值 | 位置 |
| --- | --- | --- |
| `tileSizeLimit` | **`"2MB"`** | `TileSliceParameter:109`、`MultiLayerTileSliceParameter:75` |
| `featureLimit` | `null`（且 `featureLimitEnabled = false`） | `TileSliceParameter:87/106` |
| `hardFeatureLimit` | 由调用方按图层给（管理端为 8000） | 聚合阶段的安全阀，不是同一个东西 |

**两点要注意**：

1. **字节限额是 tippecanoe 的 4 倍**（2,000,000 vs 500,000）。这个值由任务参数下发、
   也存在历史任务记录里，直接改默认值会改变既有产出的体积与降级触发点。
   是否对齐 500K 是产品取舍，不是纯技术问题 —— 记在第八节。
2. **`featureLimit` 与 tippecanoe 的 `-O` 不是一回事**。V3 的 `featureLimit` 只在
   `featureLimitEnabled = true` 时生效；不启用时，瓦片内的要素数由**聚合阶段的安全阀**
   （按图层的 `hardFeatureLimit`）决定，而且那个安全阀是 `reduceByKey` 热路径上的
   兜底削减，语义与 `-O` 的"单瓦片要素上限"并不对应。

### 超限策略开关的生效条件（核对中发现的实现问题）

`coalesceDensestAsNeeded` / `dropDensestAsNeeded`（对应 `-aD` / `-as`）被实现在
`applyFeatureLimits`（"按图层要素数上限削减"）里，而这个方法第一步就把没开要素数限制的图层跳过：

```java
// MultiLayerMvtEncoderV3.applyFeatureLimits
if (rows == null || rows.isEmpty() || !layer.isFeatureLimitEnabled()) {
    continue;                       // 没开要素数限制 → 整层跳过
}
```

两个开关的**全部读取点**（`grep` 核实）：

| 开关 | 读取点 | 触发条件 |
| --- | --- | --- |
| `coalesceDensestAsNeeded` | `applyFeatureLimits:125` | 仅 `featureLimitEnabled = true` |
| `dropDensestAsNeeded` | `applyFeatureLimits:129` **+ `selectForTrim:295`** | 前者同上；**后者由瓦片字节超限驱动，与 `featureLimitEnabled` 无关** |

所以**两者性质不同**：

- `coalesceDensestAsNeeded` 只有一处读取点，且在 `-f` 门槛后面 → **默认配置下永不执行**。
- `dropDensestAsNeeded` 在字节超限那条降级路径上是**会生效**的（`selectForTrim` 用它决定
  "按密度挑"还是"按屏幕占用取前缀"）。但默认 2MB 限额下瓦片通常不超限，所以实际也没跑过。

**而默认值两处都是 `true`**，前端也把它们**默认勾选**展示给用户（标签写作"超限策略：
优先合并高密度区域的要素 / 优先丢弃高密度区域的要素"）：

| 位置 | 默认值 |
| --- | --- |
| `MvtLayerSpec:58/61` | `coalesceDensestAsNeeded = true`、`dropDensestAsNeeded = true` |
| `task.js:119/120`（新建图层默认对象） | 同上，且保存时用 `!== false` 归一化，结果仍是 `true` |
| 而 `featureLimitEnabled` | `false`，`featureLimit` 为空 |

**结果：用户看到这两个开关是勾上的，"合并"那个其实从来没跑过**（本次实测的 poi 任务解析结果
正是 `coalesceDensestAsNeeded: true` + `dropDensestAsNeeded: true` + `featureLimitEnabled: false`）。

顺带一处语义上的提醒（tippecanoe README 原文）：

- `-as` `--drop-densest-as-needed`：*"If a tile is too large, try to reduce it to under 500K by
  **increasing the minimum spacing between features**. The discovered spacing applies to the
  entire zoom level."*
- `-aD` `--coalesce-densest-as-needed`：*"...combine the densest features from each zoom level into
  other nearby features to keep large tiles under the 500K size limit. (Again, **mostly useful for
  polygons**.)"*

也就是说**点数据要压低密度，tippecanoe 用的是 `-as`（增大间距）或 `-aC`/`-K`（聚类并留占位点），
而不是 `-aD`**。V3 这边 `coalesceBySpatialDensity` 的做法是"每个 10×10 网格内的要素合成一个
MultiPoint"，更接近 `-aC` 的聚类语义，而不是 `-aD` 的"并入邻近要素"。
这一层对应关系要不要重定，记在第八节。

---

## 五、硬性约束

### 5.1 不能影响 V1 / V2 的行为（最高优先级）

用户原话：

> **"完全不能影响到 v1 和 v2 的行为！"**

这不是泛泛的口号，有具体事实支撑。以下共用类**同时被 V1/V2 与实时切片使用**，
改动会顺着调用链流出去（引用关系已在 `V3切片改动说明.md` 中逐个核对过行号）：

| 共用类 | 谁在用 |
| --- | --- |
| `geoair-mvt-tools/.../mvt/tools/AdvMvtDensityUtils.java` | V1/V2（`VectorTileCommonUtils`、`V2VectorTileUtils`）、**实时切片**（`geoair-real-mvt` 的 `AbstractVectorTileExecutor`） |
| `geoair-mvt-tools/.../mvt/tools/PipelineBuilder.java` | V3 编码器、**实时切片**（`VectorTileBuilderConsumerByJts`） |
| `geoair-mvt-tools/.../mvt/tools/model/FeatureWithGrid.java` | `AdvMvtDensityUtils` 内部模型 |
| `geoair-static-mvt-spark/.../impl/v3/archive/PmtilesUtils.java` | V3 归档流程；内部结构为 `private` |

### 5.2 不能影响实时切片（`geoair-real-mvt`）

`geoair-real-mvt` 与静态切片是两条独立链路，但**共用上面几个类**。
V4 的任何改动都不得改变实时切片的产出或行为。

### 5.3 共用代码的三条出路

V4 若需要共用代码的帮助（如 `AdvMvtDensityUtils`、`PipelineBuilder`、`PmtilesUtils`），只有三条路：

1. **不动它们**；
2. **V4 自己实现一份**；
3. 若要改动，**改动必须是行为等价的**（不改变 V1/V2 与实时切片的产物）。

### 5.4 本轮已有的先例：V3 的性能修复全部收回

这是**用户期望的做事方式**，有完整的事实经过：

- V3 本轮改造**最初**把若干能力加在 `AdvMvtDensityUtils`、`PipelineBuilder`、
  `model/FeatureWithGrid`、`PmtilesUtils` 这四个**共用类**上；
- 后来这些改动**被全部收回**，改为在 V3 自己的包内新增 `V3FeatureUtils` / `V3PmtilesReader` 承接；
- 收回后，四个共用类的工作区版本与 `HEAD` 版本**逐字节一致**
  （`git diff` 输出 **0 字节**，`git hash-object` 与 `git rev-parse HEAD:<path>` 的 blob 哈希完全相同）；
- V3 宁可**多维护一份相似实现**，也不去动共用类的语义。

具体的"宁可自己写"的两个理由（写在 `V3FeatureUtils` 类注释里，也是判断标准）：

- **排序关键字必须一次性算好并缓存**：共用实现在比较器里现算几何 WKT（`Geometry.toText()`），
  比较器被调用 O(k log k) 次；要改这一点就要动共用类。
- **要素数削减必须有下限**（最多削到原数量的 1%、且不少于 200 个）：共用版本**没有下限概念**，
  改它就会改变 V1/V2 的裁剪结果。

由此形成的设计原则（同样适用于 V4）：

1. 新能力一律落在自己的包内，**共用类保持只读**；
2. **共用类里"顺手能改好"的东西，只要会影响 V1/V2 的产物，就不改**；需要不同语义时，自己写第二份实现；
3. 共用类只接受"缺陷修复"性质的改动，且必须逐项列出影响；
4. 判断标准不是"改起来省不省事"，而是"**V1/V2 与 `geoair-real-mvt` 的产物会不会变**"。

> 需要一并知悉的是：本轮确实有 **2 处共用代码的真 bug 修复被保留**（`TileIterator` 的
> 整级静默跳过、`TileConverter3857Utils` 的边界瓦片少算）。这两处会改变 V1/V2 的产物
> （重新切片会产出比历史更多的瓦片），用户看过说明后**明确选择保留**。
> 这是"缺陷修复"这一例外的既有案例，**不是**"可以随意改共用类"的先例。

---

## 六、V4 必须解决的两个真实问题

### 6.1 严重缺陷：瓦片漏切（**当前构建已不复现，历史复现产物无法取证**）

**现象**：用约 **76 万个点**的 poi 数据集切 **z6–z9**，有大量"应有"的瓦片没被切出来。

`工作总结.md` 记录的原始观测数据：

| 项目 | 数值 |
| --- | --- |
| 缺失瓦片总数 | 68 个（**注意：这个数字的成立与否尚未定论，见下**） |
| 覆盖源数据点占比 | 89.5% |
| 最刺眼的一格 | `z9/416/214`：211361 个源点（占全数据集 27.7%）却完全没有瓦片 |
| 逐层分布 | z6 缺 2、z7 缺 3、z8 缺 14、z9 缺 49 |
| 缺失形状 | **内部成片空洞**（z9 产出区域填充率仅 0.236，34 个内部空洞） |

**关键判断（未变）**：这**不是边界效应**。"数据集覆盖范围有限"解释不了内部成片空洞。

**已排除的假设**（都做过可复现检验）：数据读取截断、丢整个 Spark 分区、按索引区间丢弃、
编码阶段清空、空瓦片被跳过、y 轴弄反、`TileIterator` 修复未生效
（已用 `javap` 反编译运行中 jar 确认修复在）。

---

#### 6.1.1 本轮重跑结果：当前构建不再复现

用修复后的构建按原口径重跑 **z6–z9**（poi、2MB 限额、gzip、4490 与 3857 各一次），
再用源数据算出"每个含数据的瓦片"逐一核对产出：

| 网格 | z6 | z7 | z8 | z9 | 合计 | 漏切 | 耗时 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 4490 | 4/4 | 8/8 | 22/22 | 58/58 | 92 块 / 37.3MB | **0** | 122s |
| 3857 | 4/4 | 9/9 | 23/23 | 66/66 | 102 块 / 40.4MB | **0** | 116s |

（表格为"应有/产出"，判定方式：按矩阵集公式把每个源点映射到瓦片，得到"含数据的瓦片集合"，
再与该层级实际产出求差。4490 用 `2^z` 列 × `2^(z-1)` 行；3857 用标准 Web Mercator。）

**原始观测里那个 `z9/416/214`**（3857 下 x=416 → 112.5°E，y=214 → 28.4°N，确在数据范围内）
在本次产出里存在：**8,683 个要素、每个 24 个属性、压缩后 1.17MB**。

**内容层面也没有成片空洞**（按 0.25° 格统计，两套网格 z6–z9）：

| 层级 | 4490 中位保留率 | 3857 中位保留率 | 源点数 ≥500 却零保留的格 |
| --- | --- | --- | --- |
| z6 | 2.45% | 2.63% | 0（两套均 0） |
| z7 | 2.91% | 2.96% | 0 |
| z8 | 7.24% | 8.07% | 0 |
| z9 | 22.69% | 27.00% | 0 |

零保留的格剩下 1~23 个，但它们**源点数都在 1~149 之间**（对照整体保留率，
期望保留数本来就不到 10 个），属抽样噪声，不是成片缺失。

> **顺带说明一处容易误判的差异**：旧构建会把范围内**没有数据的瓦片也写成空占位文件**
> （实测旧 4490 产物 `1789726706081-5` 在 z6/z7/z8/z9 分别多出 5/7/11/20 个占位，
> 还包含 x=51 这类明显在数据范围外的键）；当前构建跳过空瓦片，只写有内容的。
> 所以拿新旧产物对比时，"新产物看起来有窟窿"未必是漏切 —— 先看那块**本来有没有数据**。

#### 6.1.2 历史记录对不上，但有据可查的一点

库里留有三次同样口径的历史任务（都是 poi、z6–z9、3857、POSTGRESQL 输出）：

| task_id | 任务名 | 写出瓦片 |
| --- | --- | --- |
| `1789742800028-3` | poi-size-limit-rerun | **145** |
| `1789746128236-2` | poi-emptyfix-check | **32** |
| `1789748827251-2` | poi-ladder-check | **34** |

**`32` / `34` 与本次的 `102` 差了约三倍。** 需要说明的是：

- 这三次任务的参数与本次不同（POSTGRESQL 输出、`100KB` 限额、不 gzip、
  `featureLimit = 2000` + `coalesceDensestAsNeeded` + `dropSmallestAsNeeded` + `generateIds`）。
  但**参数不是原因** —— 已用它们的原参数（仅把输出改成本地目录）重跑一次，当前构建仍写出 **102 块**。
- 那三次的产出目录不在磁盘上（POSTGRESQL 输出），**无法逐瓦片比对**，因此只能说
  "当前构建不复现漏切"，**不能反推"当时那 32 块里缺的就是这 70 块"**。

#### 6.1.3 仍未定论的部分（保留）

**"应有 145 格"这个基准数字本身可能是过计数的**。代码侧已核实两层"向外扩"：

- `TileConverterCommon.tileRangeByGeom` 对点要素加 `POINT_OFFSET = 0.0001` 偏移，
  使点变成一个 `2 * 0.0001` 宽的小方框；
- `tileRangeByBox` 用 `Math.floor(... - PRECISION)` / `Math.ceil(... + PRECISION)`
  **向外取整**，且 `ceil` 之后**没有再减 1**。

两层叠加，导致**一个点会被恒定撑成 4 格**（或至少多算邻格）。
`TileConverter3857Utils.tileRangeByBox` 也有同样的写法。方向是**多算不漏**，
所以它解释不了"漏"，但会让"应有"这个分母偏大 —— 这正是 145 这个数字可疑的原因。

**V4 的目标（这一条明确，不受上面的定论影响）**：
**覆盖必须完整，不能有内部空洞。** 无论最终定论是 68 格、34 格还是别的数字，
"数据明明覆盖到了、却没有产出瓦片"这件事本身就必须被消除。

### 6.2 低层级网格算错（**已修复**）

> 本节状态：**修复已完成并验证**。以下先保留原始现象记录（便于对照），
> 再写根因、修复与需要订正的地方。完整取证见 `低层级网格退化分析.md`。

**原始现象记录**：两套网格（3857 与 4326/4490）在 **z < 6** 时算出的瓦片范围是**错的**。

- **3857**：z=5 时算出 **3.88e7 米**，**超出 `±20037508`**（Web Mercator 有效范围，
  即 `MAX_MERCATOR = EARTH_RADIUS * Math.PI`）；
- **4326**：z=5 时得到 **`180`** 与**负纬度**。

**用户的明确判断**：

> **"这应该是 bug，不应该啊"**

**当时的处理**：用 `MIN_VALID_ZOOM = 6` 挡住（`AplRealMvtTileService.MIN_VALID_ZOOM`，
`GeoJsonMvtTileService` 复用，`DataSetServiceImpl` 用它设 `session.setMinZoom(...)`）。
用户定性为"**用限制遮住错误**"——错误还在，只是不让它暴露。

---

**根因（实测定位，不是推演）**：

问题只在一套实现上 —— 生产实际在用的 **`Wgs84EqualAxisTileUtils`**（等轴）。
`sdk` 里另有一套 `Wgs84SeparateAxisTileUtils`（非等轴），写法是对的。

1. **Y 上限误用列数**（`Wgs84EqualAxisTileUtils.tileRangeByBox`）：

   ```java
   int maxTileIndex = (1 << z) - 1;   // 同一个值当 x 与 y 的上限用了
   ```

   x 用它是对的（列数确为 `2^z`），y 是错的 —— 这套网格的行数只有 `2^(z-1)`。
   于是**从 z=1 起每一级**都会反算出不存在的行号，而这些行号换回纬度是**零高度的空范围**
   （z=2 时反算到 `y[0..2]`，真实只有 2 行；z=5 时 32 列 16 行却放出 `y[0..31]`）。
   另一半用 `getNumTilesHigh() - 1` 的非等轴实现恰好写对了。

2. **纬度被裁到 ±85.0511287798**（Web Mercator 有效纬度），与底图在用的
   `EPSG:4326_19` 矩阵集对不上。该矩阵集定义（WMTS `GetCapabilities` 实取）：
   `TopLeftCorner` 纬度 90、`MatrixWidth = 2^z`、`MatrixHeight = 2^(z-1)`、覆盖 **±90°**。
   瓦片按 170.1° 归一化、前端按 180° 解释，同一个要素在纬度方向被拉伸
   （z=5 的最北/最南两行约 1.8 倍）。

3. **"z<6 才退化"这个说法不成立**。实测每级都有 50% 的 y 是无效行，
   与层级高低无本质关系。

**修复**：

| 提交 | 仓库 | 内容 |
| --- | --- | --- |
| `11bb8c84` | geoair-framework | 4326 网格统一到标准矩阵集：`GirGeoTools.getTileGrid4326Opt()` 改返 `Wgs84SeparateAxisTileUtils`；`Wgs84EqualAxisTileUtils` 标 `@Deprecated` 并继承前者；删掉只服务于 ±85.05 裁剪的 `MAX_VALID_LAT` / `MIN_VALID_LAT` |
| `dfb9571` | geoair-mvt-server | `MIN_VALID_ZOOM` 由 6 改为 **0**，低层级预览放开 |

**验证**（探针在临时目录：

```text
   z  列数  行数  期望行 | 首行纬度         末行纬度          | 全域反算
   3    8    4     4    | [45.0000,90.0000] [-90.0000,-45.0000] | x[0..7]   y[0..3]
   5   32   16    16    | [78.7500,90.0000] [-90.0000,-78.7500] | x[0..31]  y[0..15]
  12 4096 2048  2048    | [89.9121,90.0000] [-90.0000,-89.9121] | x[0..4095] y[0..2047]
```

- 判据对着底图矩阵集 `EPSG:4326_19` / `EPSG:3857_19` 断言，z=0..12 共 **103 项全过**
- 两条 4326 入口逐级逐瓦片比对，输出逐行一致
- 3857 本次未改，逐级自洽（原记录的"3857 z=5 得 3.88e7"来自用了越界索引 `x=47`，
  z=5 的合法 x 只有 0..31；`validateXyz` 有意不校验 `x < 2^z`，因为 `x+1` 要参与右边界计算）
- 端到端：GeoJSON 数据集在 4490 与 3857 下 z=0~6 均能出瓦片，不含数据的瓦片仍返回 0 字节

**需要订正的两处原始描述**：

1. 「**4326：z=5 时得到 180 与负纬度**」——实测 z=5 是 32 列 16 行，那两行正常的。
   真正的缺陷是上面第 1 条（y 上限）与第 2 条（±85.05），不是"得到 180 与负纬度"。
2. 「**3857 z=5 时 x=47 → 3.88e7**」——数字算得没错，但 `x=47` 本身就超出 z=5 的
   合法范围（0..31）。想在上游拦住这类越界索引，是**另一个可加的改动点**，与本节无关。

**V4 的要求（已满足，保留作为回归判据）**：
低层级瓦片范围必须算对，不允许用 `MIN_VALID_ZOOM` 一类方式回避；两套网格都要正确。

---

## 七、质量取向

以下四条不是具体功能，而是从本轮互动中提炼出的**用户对"做得对不对"的判断标准**，
V4 应同样遵守。

### 7.1 不接受牺牲可用性的取舍

上一版曾采用"整层清空"的做法：单个 PBF 超限时，**宁可把这一格瓦片清空**也要守住大小上限。
**用户明确否决**：

> **"宁可不给这一格瓦片，也不能超过上限，这个逻辑是不对的！！严重影响用户体验！"**

**期望的正确逻辑**：
**降级可以，但绝不能丢格、绝不能让地图出现空洞。**
具体落到 V3 已经实现的做法上（`MultiLayerMvtEncoderV3` 的超限降级阶梯），
"先降精度、再减数量"逐级降级，走到最底仍超限也**照常输出并打 WARN**：

| 级别 | 手段 | 要素是否丢失 |
| --- | --- | --- |
| 1 | 逐级提升简化级别，最多 `tileSizeOptimizeRounds` 轮 | 一个不丢，只降精度 |
| 2 | 裁掉属性，只留 id 与关键字段 | 一个不丢，几何不动 |
| 3 | 面/线转质心点（几何降级） | 每个要素仍在图上，只是变成点 |
| 4 | 按图层优先级降要素数（**每层有下限**） | 开始减数量，但每层不低于下限 |
| 5 | 仍超限则**照常输出 + WARN** | **绝不丢格、绝不清空** |

第 4 级的下限：最多削到原数量的 **1%**（`TRIM_FLOOR_RATIO = 0.01`），
且不少于 **200** 个（`TRIM_FLOOR_MIN = 200`），**保证图层不会被削空**。

只有"所有图层都没有要素可写"的瓦片才返回空——那是数据本身就没有覆盖到，不是因为超限。
空瓦片不写出（`isBlankTile`），且不计入写出统计。

### 7.2 不接受用限制掩盖未知

`MIN_VALID_ZOOM = 6`（见 6.2）是典型：错误还在，只是不让它暴露。
期望是**把错误修对**，而不是**加一个限制让人碰不到它**。

### 7.3 重视如实汇报

本轮多次出现"**声称完成但实际未验证**"的情况。用户在意的是**状态要分得清**。
`工作总结.md` 已经为此立了标准：明确区分
"**已完成且已验证 / 已完成但未验证 / 未完成**"三种状态。
V4 的进展汇报应沿用这个标准——做了什么、验证到什么程度、哪些没验证，必须分开写。

### 7.4 定位不到根因时，宁可说"没定位到"

不要给似是而非的结论。本轮漏切缺陷就是一个正面例子：
在根因未定位时，文档如实写"根因尚未定位"，并把新线索明确标注为
"**未定论线索**，不是结论"。V4 遇到同类情况应同样处理。

---

## 八、待确认的开放问题

以下问题**用户尚未表态**，本文档**不替用户决定**，列出供后续确认：

1. **V4 与 V1/V2/V3 是并存还是替代？**
   是新增一条独立实现（V1/V2/V3/V4 并存，由使用方选择），还是 V4 成熟后替代 V3（甚至更早的版本）？
   这直接决定 V4 需要付出多少兼容成本。

2. **是否需要同样支持管理端接入？**
   V3 已经贯通到 `geoair-mvt-server`（`MvtLayerSpec` / `TileParameterBuilder` / 任务 UI / 成果预览）。
   V4 是否要复用同一套管理端参数模型与 UI，还是另起一套？
   ticket 的 Base32 编解码约定是否需要保持兼容？

3. **检测与迁移策略？**
   V4 完成后，已有的 V3 任务 / 瓦片成果如何处置——重新切片、并存、还是只对新任务走 V4？

4. **共用代码的处置边界。**
   第五节给了三条出路（不动 / 自己写 / 行为等价地改）。若 V4 与 V3 需要长期并存，
   两套自建实现（如 `V3FeatureUtils` 与 V4 的对应物）的维护方式需要有明确约定。

5. **低层级网格修复的回归验证。**
   6.2 已修复（`11bb8c84` + `dfb9571`）。但 4326 网格的既有产出在最北/最南两行的
   纬度范围会从 ±85.05 变成 ±90，**V1/V2/V3 的切片产物回归尚未跑**——
   需要确认差异只落在那两行、其余格子与层级不变。3857 不受影响（本次未改）。
   在这条跑完之前，本次修复的影响面只能算"已评估、未实测"。

6. **技术可行性本身。**
   "脱离 Spark 实现 tippecanoe 全部能力"是否可行、代价如何、是否需要引入外部依赖
   （如直接调用 tippecanoe 二进制），属于可行性分析文档的范畴，本文档不作判断。
   截至本文撰写时 `V4脱离Spark可行性分析.md` 尚未产出。

7. **降级决策要不要做成 zoom level 级？**
   第四节"作用域差异"指出：tippecanoe 的多数降级参数（`-ad` / `-an` / `-aN` / `-aS` / `-aG`）
   是**跨瓦片统一**的 zoom level 级决策，而 V3 全部是瓦片级。V4 是否要引入 zoom level 级决策层，
   属于架构层面的取舍——它既决定 V4 能否真正"具备 tippecanoe 的完整能力"，
   也直接影响脱离 Spark 之后的实现方式（需要一次全层扫描并保持跨瓦片状态）。

   **用户已表态（见一之二第 3 条）：方向就是要"从全局的角度"，所以答案是"要做"。**
   尚未定的是做法与代价：全层预扫的内存/时间开销、与现有"边读边分配"流程的衔接、
   以及哪些参数先做（`-ad` 这类只影响丢弃比例的，比 `-aG` 这种要改 gamma 的容易落）。
   这一步没做之前，V4 只能说"聚合削减是全局确定性的"，不能说"降级是全局的"。

8. **单瓦片字节上限对齐到多少？**
   tippecanoe 默认 500K，V3 默认 2MB（第四节"默认限额差异"）。改默认值会改变既有产出的
   体积与降级触发点，属产品取舍。V4 若以"具备 tippecanoe 能力"为目标，需要明确
   是"能力对齐、默认值各自保留"，还是"默认值也一并对齐"。

9. **超限类开关的生效条件与语义对应是否重定？**
   第四节"超限策略开关的生效条件"指出：`coalesceDensestAsNeeded` 只挂在 `-f` 门槛后、
   默认配置下从不执行；`dropDensestAsNeeded` 多一条字节超限路径但默认 2MB 下也不跑。
   而前端把这两个开关**默认勾选**、界面标签写的是"超限策略"，用户勾了却看不到效果。
   要定的是两件事：
   - **触发条件**：继续挂在 `-f`（要素数上限）下面，还是改成由瓦片字节超限（`-M`）驱动
     —— 后者才是 tippecanoe 的语义；
   - **语义对应**：tippecanoe 里点数据压低密度用的是 `-as`（增大最小间距）与
     `-aC`/`-K`（聚类并留占位点），`-aD`（coalesce-densest）官方注明"mostly useful for
     polygons"；而 V3 的 `coalesceBySpatialDensity` 是"每格合成一个 MultiPoint"，
     更接近聚类的语义。三个开关名与实现的对应关系需要理一遍。
   本项与第 7 条（zoom level 级决策层）相邻，但可以在瓦片级之内先做。

---

## 九、附录：相关文档索引

| 文档 | 路径 | 状态 |
| --- | --- | --- |
| V3 切片改动说明（667 行） | `H:\gitee\geoair-root\geoair-framework\V3切片改动说明.md` | 已提交（`02226d5d`），描述 V3 本轮改动全貌 |
| 工作总结（本轮） | `H:\gitee\geoair-mvt-server\工作总结.md` | 未提交，含漏切缺陷、网格边界实验、未验证清单 |
| 今日完成功能说明（297 行） | `H:\gitee\geoair-mvt-server\今日完成功能说明.md` | 已提交（`e3fd3dd`），APL 侧功能说明 |
| V4 脱离 Spark 可行性分析 | `H:\gitee\geoair-root\geoair-framework\V4脱离Spark可行性分析.md` | **尚不存在**（撰写本文时已确认目录下无此文件） |
| 低层级网格退化分析（实测报告） | `H:\gitee\geoair-root\geoair-framework\低层级网格退化分析.md` | 未提交；6.2 的取证记录（探针数据、根因定位、修法与影响面） |
| **V4 期望（本文档）** | `H:\gitee\geoair-root\geoair-framework\V4期望.md` | 需求/期望记录 |

### 关键代码位置索引（本文档核实过的路径）

均相对于 `H:\gitee\geoair-root\geoair-framework\`：

| 内容 | 路径 |
| --- | --- |
| V3 参数 DTO（10 个文件） | `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/dto/v3/` |
| V3 实现 | `.../vectile/impl/v3/`（`MultiLayerMvtEncoderV3`、`SparkVectorTileGeneratorV3`、`V3FeatureUtils`、`V3PmtilesReader`、`V3TileWriteStats`、`V3TileFeatureGroup`、`input/`、`output/`、`archive/`） |
| 3857 网格转换（共用） | `geoair-modules/geoair-geo/geoair-geo-tools/src/main/java/cn/geoair/map/dynamic/tools/grid/converter/TileConverter3857Utils.java` |
| 4326/4490 网格转换（共用） | `.../grid/converter/Wgs84SeparateAxisTileUtils.java`（常量在 `AbstractWgs84TileConverter.java`） |
| 瓦片范围公共入口（`POINT_OFFSET`） | `.../grid/converter/TileConverterCommon.java` |
| 方言工厂（9 类数据库） | `geoair-modules/geoair-geo/geoair-adv-query/src/main/java/cn/geoair/map/dynamic/adv/spring/AdvExecutorFactory.java` |

调用方仓库 `H:\gitee\geoair-mvt-server\`：

| 内容 | 路径 |
| --- | --- |
| 管理端图层参数 | `mvtserver-boot/mvtserver-core/src/main/java/cn/geoair/girteam/mvtserver/core/dto/MvtLayerSpec.java` |
| 参数翻译 | `.../core/support/TileParameterBuilder.java` |
| 实时切片（`MIN_VALID_ZOOM`） | `.../core/support/AplRealMvtTileService.java`、`.../core/support/GeoJsonMvtTileService.java` |
| 前端 tippecanoe 参数表单 | `mvtserver-boot/mvtserver-wcs/src/main/resources/static/console/pages/task.js` |
