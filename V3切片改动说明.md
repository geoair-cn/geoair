# V3 多图层切片改动说明

> 本文档描述 **`geoair-framework` 仓库当前工作区（尚未提交）的真实状态**。
>
> 本文档已按一次重要的**隔离重构之后的最终状态**重写。重构之前，本轮改造曾把若干能力加在
> `AdvMvtDensityUtils`、`PipelineBuilder`、`model/FeatureWithGrid`、`PmtilesUtils` 这四个**共用类**上；
> 重构之后这些改动**已被全部收回**（见第二节与第七节的逐字节确认），同样的能力改由 **V3 自己包内新增的
> 工具类**（`V3FeatureUtils` / `V3PmtilesReader`）承接。因此早先文档中"共用类新增读取能力""共用类排序缓存"
> "共用类 null 兜底"等描述**均已失效**，请以本文档为准。

---

## 一、总览

- **仓库**：`H:\gitee\geoair-root\geoair-framework`（独立 git 仓库）
- **分支**：`feature/J8-dev`
- **基线提交**：`2c670e4913de483778f7be4b0b2453827b5fa7aa 修复PostGisGeometryAdvTypeHandler的空间类型判断的问题`
- **改动姿态**：**全部改动都在工作区，未做任何 `git add` / `git commit` / `git stash`**

### 改动规模（数字均来自真实命令输出，未做估算）

`git diff --numstat` 与 `git diff --stat` 的输出：

| 项目 | 数值 |
| --- | --- |
| 修改文件数 | **7** |
| 修改文件增删 | **`699 insertions(+), 94 deletions(-)`** |
| 新增（未跟踪）源文件数 | **3** |
| 新增源文件行数 | **635 行**（`V3FeatureUtils` 239 + `V3PmtilesReader` 327 + `V3TileWriteStats` 69） |
| `git status --short` 行数 | **11**（7 个未暂存修改 ` M` + 4 个未跟踪 `??`：3 个新增 `.java` + 本文档自身） |

### 本轮主线

把 Spark V3 切片做成 **tippecanoe 大部分能力的复刻实现**：补齐 tippecanoe 风格的图层/任务控制参数；
在编码器里实现"属性裁减 → 几何降级 → 点聚合 → 要素排序裁剪 → 超限降级阶梯"这条完整的优化链；
修掉一个会让百万级数据卡死的**平方级性能缺陷**；顺带修正空瓦片写出、补上写出统计；并保留两处共用代码的
**真 bug 修复**（会改变 V1/V2 产物，见主题 7 的显著标注）。

### 最重要的结论：共用类已全部收回

以下是**被有意保留、必须写进结论**的一条设计约束：

> **本轮 V3 的改动只落在 V3 自己的包（`impl/v3`、`dto/v3`）里。任何共用类都没有被 V3 的优化需求改动过。**

四个共用类的当前工作区版本与 `HEAD` 版本**逐字节一致**（`git diff` 输出 **0 字节**，`git hash-object` 与
`git rev-parse HEAD:<path>` 完全相同），详见第七节的确认表。

---

## 二、隔离原则：V3 不得改动共用代码

### 2.1 起因

用户对本轮改造提的硬性要求是"**完全不能影响到 V1/V2 的行为**"。这不是一句泛泛的口号，而是有具体事实支撑的：

| 共用类 / 共用文件 | 谁在用（已核对真实引用） |
| --- | --- |
| `geoair-mvt-tools/.../mvt/tools/AdvMvtDensityUtils.java` | V1/V2（`vectile/utils/VectorTileCommonUtils.java` 第 194、204、245、248 行；`impl/v2/V2VectorTileUtils.java` 第 70、80 行）、**实时切片**（`geoair-real-mvt/.../mvt/exec/AbstractVectorTileExecutor.java` 第 305 行） |
| `geoair-mvt-tools/.../mvt/tools/PipelineBuilder.java` | V3 编码器、**实时切片**（`geoair-real-mvt/.../consumer/VectorTileBuilderConsumerByJts.java` 第 43 行） |
| `geoair-mvt-tools/.../mvt/tools/model/FeatureWithGrid.java` | `AdvMvtDensityUtils` 内部使用的模型 |
| `geoair-static-mvt-spark/.../vectile/impl/v3/archive/PmtilesUtils.java` | V3 归档流程；其目录条目等**内部结构是 `private`** |

也就是说：**只要动这几个类，改动就会顺着调用链流到 V1、V2 与 `geoair-real-mvt` 实时切片**。

### 2.2 落地方案：V3 自己的包内实现

本轮把这些优化需求**全部收回**，改为在 `impl/v3` 包内独立实现：

**1) `V3FeatureUtils.java`（新增，239 行，包级私有 `final class`）**
承接原先想加在 `AdvMvtDensityUtils` 上的四类能力：`gzip` 压缩、按空间密度合并、按密度丢弃、按屏幕占用排序。
它被 `MultiLayerMvtEncoderV3` 使用（`coalesceBySpatialDensity` / `filterBySpatialDensity` / `gzip`）。

独立实现的**两个具体理由**（写在类注释里，也是判断标准）：

- **排序关键字必须一次性算好并缓存**。共用实现的做法是在比较器里**现算**几何 WKT（`Geometry.toText()`），
  比较器会被调用 O(k log k) 次，要素一多排序本身就成瓶颈；要改这一点就要动共用类。
- **要素数削减必须有下限**（"最多削到原数量的 1%、且不少于 200 个"，落在编码器
  `MultiLayerMvtEncoderV3.resolveTrimFloor`）；共用版本**没有下限概念**，改它就会改变 V1/V2 的裁剪结果。

两者都改，就必然会动到 V1/V2，所以宁可多一份实现。

此外，这份自建实现还顺手修掉了共用实现在密度网格划分上的一个语义问题：共用实现对每个要素用它**自己的外接
矩形**当"瓦片范围"来算网格（对点要素等于除以零、网格划分退化）；`V3FeatureUtils` 改为**直接用瓦片范围**
划分网格（`GRID_SCALE_PROJECTED = 100.0` / `GRID_SCALE_GEOGRAPHIC = 10.0`），语义更明确、结果更可预测。

> 备注：`V3FeatureUtils.pipeline(Envelope, int)` 是一个给调用方构造坐标变换管线的**薄封装**，
> 当前**没有任何调用方**（`MultiLayerMvtEncoderV3` 直接用 `PipelineBuilder.newBuilder(...)`），属于预留接线点。

**2) `V3PmtilesReader.java`（新增，327 行，包级私有 `final class`）**
PMTiles V3 归档的**按需读取**实现：只读 Header + 根目录，必要时再进一层叶子目录；支持 run-length 连续段、
varint 目录段解析、gzip 判断与解压；XYZ → PMTiles TileID 用与写入端同一套 Hilbert 累计编号算法；
另外提供 `contains(...)` 与 `readMetadata(...)`（目录条目 `offset == 0` 表示不占数据段，非零表示"实际偏移 + 1"）。

独立实现的理由：共用类 `PmtilesUtils` **只负责写出归档**，且它的目录条目等内部结构是 `private`，
V3 不该为了"读一个瓦片"去改它。

> 备注：`V3PmtilesReader` 当前在本仓库与调用方仓库（`geoair-mvt-server`）中**都没有调用方**——
> 预览侧的 PMTiles 读取目前由调用方仓库自己的 `PmtilesTileReader` 承担，本类是已落地、待接线的能力。

### 2.3 把这条约束写成设计原则

1. **V3 的新能力一律落在 `impl/v3` / `dto/v3` 包内**，共用类保持只读。
2. **共用类里"顺手能改好"的东西，只要会影响 V1/V2 的产物，就不改**；V3 需要不同语义时，在 V3 包内写第二份实现。
3. **共用类只接受"缺陷修复"性质的改动**，且必须逐项列出影响（本轮只有 2 项，见主题 7）。
4. 判断标准不是"改起来省不省事"，而是"**V1/V2 与 `geoair-real-mvt` 的产物会不会变**"。

### 2.4 代价与收益

| | 说明 |
| --- | --- |
| **代价** | 多一份与共用实现相似的代码：`V3FeatureUtils` 的密度合并/丢弃/排序，与 `AdvMvtDensityUtils` 的能力存在重叠，将来若要统一语义需要同步维护两处 |
| **收益** | **V1/V2 与 `geoair-real-mvt` 实时切片对本轮 V3 优化零风险**：V3 后续再调简化尺度、密度网格、裁剪下限、排序策略，都不需要重新回归 V1/V2 的产物 |

---

## 三、按主题的改动说明

### 主题 1：V3 参数补齐 tippecanoe 风格控制项

**解决什么问题**：V3 原先只有简化等级、要素数上限等少量参数，无法表达 tippecanoe 的字段排除、线简化开关、
共享节点保护、点聚合距离、最小要素优先丢弃、特征 id 生成、缓冲区、超限优化轮次等控制能力。

**涉及文件**

- `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/dto/v3/MvtLayerSliceParameter.java`（**+38 / -2**）
- `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/dto/v3/MultiLayerTileSliceParameter.java`（**+14 / -0**）

**图层级新增位（`MvtLayerSliceParameter`）**

| 字段 | 类型 / 默认值 | 含义 / 对应 tippecanoe 参数 |
| --- | --- | --- |
| `excludeFields` | `List<String>`，空列表 | 需要排除的输出属性字段，**优先级高于 `includeFields`**（`-x/--exclude`） |
| `simplifyLines` | `boolean`，`true` | 是否对线状几何做简化（`-ps/--no-line-simplification` 的反向开关） |
| `preserveSharedNodes` | `boolean`，`true` | 简化时是否保留与其它要素共享的节点；开启走拓扑保持简化，关闭走逐要素独立简化（`-pS/--no-simplification-of-shared-nodes` 的反向开关） |
| `coalesceDistance` | `Integer`，`null` | 点聚合距离，**屏幕单位**（瓦片边长按 4096 计），`>0` 时把过近的点要素合并为多点（`-g/--cluster-distance`） |
| `dropSmallestAsNeeded` | `boolean`，`false` | 单个 PBF 超限裁剪要素时，是否优先丢弃**屏幕占用最小**的要素；关闭时按数据读取顺序裁剪（`--drop-smallest-as-needed`） |
| `generateIds` | `boolean`，`false` | 是否为要素生成瓦片内唯一的递增 MVT 特征 id（`-ai/--generate-ids`） |

同时把 `simplificationLevel` 的注释补上 `-D/--simplification`，并把 `coalesceDistance` 的语义由原先
"空间合并距离（像素），供后续密度策略扩展使用"（实际未被读取）明确为"**屏幕单位的点聚合距离，编码前真正生效**"。

**任务级新增位（`MultiLayerTileSliceParameter`）**

| 字段 | 类型 / 默认值 | 含义 |
| --- | --- | --- |
| `buffer` | `Integer`，`8` | MVT 瓦片缓冲区，单位为瓦片内部坐标（瓦片边长固定 4096）；对应 tippecanoe `-b/--buffer`。让跨越瓦片边界的要素在相邻瓦片中都有几何，避免线/面在地图拼接处出现断口 |
| `tileSizeOptimizeRounds` | `int`，`4` | 超过 `tileSizeLimit` 时的最大优化轮次；达到上限后直接接受当前结果，避免极端数据下反复重编码拖慢任务 |

> `buffer` 的默认值 `8` **与原编码器里的硬编码值一致**（原先写死 `new VectorTileEncoder(4096, 8, false)`），
> 因此不配置时行为不变。

**对 V1/V2 的影响**：**仅 V3 生效**。两个 DTO 都位于 `dto/v3` 包，类注释明确"该对象只供 V3 使用，
不会修改或复用 V1/V2 的 `TileSliceParameter` 参数语义"；V1/V2 用的是另一个 DTO 类型。

---

### 主题 2：`MultiLayerMvtEncoderV3` 重写（编码期优化链 + 超限降级阶梯）

**解决什么问题**：让单个 PBF 在超过总大小限制时，按"**尽量少丢信息**"的顺序自动优化，而不是简单粗暴地
把要素砍掉一半；同时让图层级的字段过滤、几何简化、点聚合、特征 id 生成真正落到编码结果里。

**涉及文件**：`geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/impl/v3/MultiLayerMvtEncoderV3.java`（**+505 / -31**，本轮最大的一个文件）

**主流程（`encode`）**：三段式，顺序有意义

```
encode(tileId, group, parameter)
  ├─ 1. applyFeatureLimits   每瓦片只做一次：密度合并 → 密度丢弃 → 顺序截断
  ├─ 2. clusterPoints        每瓦片只做一次：按 coalesceDistance 合并过近的点
  └─ 3. encodeWithTileLimit  进入超限降级阶梯（可能多轮重编码）
```

要素数限制与点聚合都**只做一次**，后续所有降级轮次复用结果——这是为了避开聚合链路上的重复开销
（见主题 3 与第五节）。

**（a）属性处理**

- `getAttributes`：先在 `includeFields` 白名单过滤，再按 `excludeFields` 扣除（**排除优先**），最后把 id 字段补回。
- `getKeyAttributes`：超限降级第 2 级专用，只保留 `sysIncludeFields` 与 id 字段（跳过几何字段）；
  若图层未声明关键字段，退化为"只保留 id"。

**（b）几何简化（`simplify`）**

- 容差 = `envelope.getWidth() / EXTENT * level`（`EXTENT = 4096`），保证不同网格与层级下简化尺度一致。
- 等级 = 图层 `simplificationLevel` + 本轮额外提升的 `extraSimplify`（`Math.max(0, extraSimplify)`）。
- 线要素（`LineString` / `MultiLineString`）：受 `simplifyLines` 开关控制，走 `DouglasPeuckerSimplifier`。
- 面要素：`preserveSharedNodes = true` 走 `TopologyPreservingSimplifier`（保留共享节点，不产生缝隙）；
  关闭时走 `DouglasPeuckerSimplifier`。

**（c）点聚合（`clusterPoints` / `clusterRows`）**

- `coalesceDistance` 从屏幕单位换算回数据单位：`tolerance = tileWidth / EXTENT * distance`。
- `clusterRows` 用锚点要素线性扫描同组候选，用 `consumed[]` 标记已并入的要素避免重复产出；非点要素
  （`isPointLike` 为假）**不参与聚合、原样保留**；合并后的几何为 `MultiPoint`，坐标去重（否则 JTS 会认为多点退化），
  属性用锚点要素（`GirAdvOneRow.ofByMap(anchor)`）作模板。

**（d）要素排序裁剪（`sortedByScreenSize` + 内部类 `Location`）**

为 `dropSmallestAsNeeded` 服务：把图层列表按**屏幕占用升序**重排，后续按前缀裁剪即"先丢最小的"。
占用口径：面（`getDimension() == 2`）取 `外接矩形面积 / tileWidth²`；线取 `外接矩形对角线 / 瓦片对角线`；
点全部并列（保持原顺序）。

**（e）超限降级阶梯（`encodeWithTileLimit`）——本轮核心设计**

原则是 **先降精度、再减数量，并且绝不因为超限而丢弃整格瓦片**。

| 级别 | 手段 | 要素是否丢失 |
| --- | --- | --- |
| 1 | 逐级提升简化级别，最多 `tileSizeOptimizeRounds` 轮（下限 `Math.max(1, ...)`） | 一个不丢，只降精度 |
| 2 | `trimAttributes = true`：裁掉属性，只留 id 与关键字段 | 一个不丢，几何不动 |
| 3 | `degradeGeometry = true`：面/线转质心点（`degrade`） | 每个要素仍在图上，只是变成点 |
| 4 | 按图层优先级降要素数（`trimLowestPriorityLayer`） | 开始减数量，但**每层有下限** |
| 5 | 仍超限则**照常输出 + WARN** | **绝不丢格、绝不清空** |

第 4 级的两条硬性约束：

- **`trimLowestPriorityLayer` 每轮只削一个图层**：按 `priority` **从低到高**（`Comparator.comparingInt(getPriority)`，
  数值越大越优先保留）找第一个"仍有要素且未到下限"的图层，按 `limit / currentBytes` 等比削减；
  每轮必须真的减少（`keep >= rows.size()` 时强制 `keep = rows.size() - 1`），否则比例收敛不到限制值。
  裁剪循环有 `MAX_TRIM_ROUNDS = 12` 次的轮次上限，达到上限后由第 5 级兜底。
- **`resolveTrimFloor` 给每层设下限**：最多削到原数量的 **1%**（`TRIM_FLOOR_RATIO = 0.01`），
  且不少于 **200** 个（`TRIM_FLOOR_MIN = 200`），**保证图层不会被削空**。
  如果图层开启了 `dropSmallestAsNeeded`，裁剪前会先把该图层按屏幕占用升序排好，于是"削"掉的正是最小的要素。

> **请特别注意第 5 级这条设计取舍**：宁可略微超限，也不让瓦片变空。理由是——**地图上出现空洞，
> 比瓦片稍微大一点严重得多**。降级阶梯一路走到最底（简化、裁属性、几何降级、按优先级削到下限）之后若仍超限，
> 代码只打一条 WARN（带瓦片 key、当前字节数、限制字节数、保留要素数）然后**照常返回结果**。
> 只有"**所有图层都没有要素可写**"的瓦片才返回空——那是数据本身就没有覆盖到，不是因为超限。

**（f）空瓦片返回空数组（`EMPTY_TILE`）**

`encodeAll` 统计 `encodedFeatures`，**没有任何要素真正写进 PBF 时返回空字节数组**；`encoder.encode()`
产出的字节长度为 0 时同样按空瓦片处理。调用方据此跳过写出（见主题 4）。

**（g）`buffer` 与 `EXTENT` 参数化**

`VectorTileEncoder` 由原先的硬编码 `new VectorTileEncoder(4096, 8, false)` 改为
`new VectorTileEncoder(EXTENT, buffer, false)`，`buffer` 来自 `parameter.getBuffer()`（空值兜底 `DEFAULT_BUFFER = 8`，
并通过 `Math.max(0, ...)` 防负数）。

**（h）编码期日志**

新增 `GiLogger`，在"通过简化+精简属性达标""通过简化+精简属性+几何降级达标""仍超限照常输出"三处打印
瓦片 key 与字节数，便于排查超限瓦片。

**对 V1/V2 的影响**：**仅 V3 生效**。`MultiLayerMvtEncoderV3` 是 V3 专属编码器，V1/V2 各有自己的编码路径
（`V2VectorTileUtils` / `VectorTileCommonUtils`），不经过本类。

---

### 主题 3：性能缺陷修复（聚合期的平方级开销）

**解决什么问题**：`V3TileFeatureGroup.merge` 原先在 Spark `reduceByKey` 的**每一次合并**里都调用密度裁剪，
而那条链路（要素数限制 → 密度过滤 → 排序比较器 → 几何 WKT 序列化）被放大成**平方级开销**，百万级数据直接卡死。

**涉及文件**

- `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/impl/v3/V3TileFeatureGroup.java`（**+24 / -28**）
- `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/impl/v3/MultiLayerMvtEncoderV3.java`（`applyFeatureLimits` / `clusterPoints`，见主题 2）
- `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/impl/v3/V3FeatureUtils.java`（**新增**，排序关键字缓存的最终落点）

**实现要点**

1. `merge` 里的 `limitLayerFeatures` 改为 **`capLayerFeatures`**：**只截断、不排序、不做密度裁剪**；
   且只在要素数超过「`hardFeatureLimit` × `CAP_TRIGGER_FACTOR`(2)」时才真正截断到 `hardFeatureLimit`，
   把"整列表拷贝一次"的开销摊薄到几乎为零。删除了 `getEffectiveLimit`（其"优先业务上限、其次安全上限"
   的逻辑在编码器侧重建为 `resolveFeatureLimit`）。
2. 要素数限制、密度合并与密度丢弃**整体挪到编码前做一次**，见 `MultiLayerMvtEncoderV3.applyFeatureLimits`
   （三层顺序：`coalesceBySpatialDensity` → `filterBySpatialDensity` → 前缀截断；且只在图层显式开启
   `featureLimitEnabled` 时才做**会改变几何**的密度合并）。
3. **排序关键字缓存的落点变了**：由于共用的 `AdvMvtDensityUtils` 与 `FeatureWithGrid` **已整体回退**，
   "把排序次关键字一次性算好、比较器只做比较"这件事**现在完全落在 `V3FeatureUtils.filterBySpatialDensity` 内部**——
   它先把每个要素的 `featureKey(row, geomField, idField)`（优先 id 字段 → 其次几何 WKT → 最后随机串兜底）
   写入私有静态类 `RowWithCell.sortKey`，再 `entries.sort(comparingInt(密度).thenComparing(预计算字符串))`。
   共用的 `AdvMvtDensityUtils` 里**没有**这个缓存，也不需要有。

**对 V1/V2 的影响**：**仅 V3 生效**（本轮改动全部在 V3 包内）。需要特别指出的是——修复前那条"共用类排序缓存"
的路径**已经不存在了**：`AdvMvtDensityUtils` 与 `FeatureWithGrid` 的 `git diff` 为空，V1/V2 与实时切片
仍然跑着它们**原来那份**实现（也就是仍在比较器里现算几何 WKT）。这**不是遗漏**，而是隔离原则的直接结果：
V1/V2 的性能问题若要修，应当作为独立议题单独评估，不放在本轮 V3 改动里。

---

### 主题 4：空瓦片不再写出

**解决什么问题**：编码器原先对没有要素的瓦片仍会返回字节（只有图层头的 PBF），生成器照常写出，
产生 `tile_data` 为空的记录，**虚高瓦片数、干扰预览与统计**。

**涉及文件**

- `.../impl/v3/MultiLayerMvtEncoderV3.java`（`EMPTY_TILE`，无要素时返回空数组）
- `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/impl/v3/SparkVectorTileGeneratorV3.java`（**+63 / -3**）

**实现要点**：生成器新增私有静态方法 `isBlankTile(pbf)`（`pbf == null || data == null || data.length == 0`），
**所有写出路径统一跳过空瓦片**：

1. **PostgreSQL**（`writeTilesToPostgresql`）：原先直接 `bindDelete` / `bindInsert`，现在先判断 `isBlankTile` 后 `continue`。
2. **本地目录 / S3 / 归档暂存目录**（`writeTilesToStore`，由 `V3TileStoreFactory.open(...)` 分派到
   `V3LocalTileStore` 或 `V3S3TileStore`）：原先遇到 `pbf == null || data == null` 会
   **抛 `IllegalStateException("V3 多图层瓦片编码结果为空，tileId=...")` 中断任务**，现在改为跳过，
   并且空瓦片**不计入写出统计**（`partitionTiles` / `partitionBytes` 都不加）。
3. **MBTiles / PMTiles 归档**（`archiveTiles`）：归档是从**暂存目录**（`stagingDirectory`）打包出来的，
   空瓦片既然没有落进暂存目录，自然也不会进入归档。

**语义变化提示（需要留意）**

- 第 2 条是**行为语义变化**：原先"编码结果为空"被视为异常并中断任务，现在视为正常情况静默跳过；
  原先靠这个异常发现问题的场景需要改用日志观察。
- PostgreSQL 路径跳过空瓦片，意味着**该 tileId 的历史记录不会被本次任务的 `delete` 清掉**。
  在同一张表上重复切片时，请确认这一行为符合预期。

**对 V1/V2 的影响**：**仅 V3 生效**。`isBlankTile` 是 `SparkVectorTileGeneratorV3` 的私有静态方法，
V1/V2 有各自的生成器，不受影响。

---

### 主题 5：写出统计 `V3TileWriteStats`

**解决什么问题**：任务结束后使用方需要知道写了多少瓦片、多少字节、归档多大，原先只能遍历输出目录或查输出表。

**涉及文件**

- **新增**：`geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/impl/v3/V3TileWriteStats.java`（**69 行**）
- `.../impl/v3/SparkVectorTileGeneratorV3.java`（`lastStats` 字段、`getLastStats()`、`archiveFileSize()`）

**实现要点**

- `V3TileWriteStats` 是 `Serializable` 的**不可变值对象**（字段全 `final`），提供
  `tilesWritten`（实际写出的瓦片数）、`bytesWritten`（瓦片数据总字节数，gzip 时为压缩后字节数）、
  `batchesWritten`（批量写入批次数）、`featuresRead`（读取到的要素数）、`archiveBytes`（归档文件最终大小，
  非归档输出为 0）；另有 `getOutputBytes()`（归档取归档大小、其余取 `bytesWritten`）与 `toString()`。
- `SparkVectorTileGeneratorV3` 新增 `lastStats` 字段，在 `doGenerate` 结束时由 Spark 累加器 `tracker`
  汇总填充；新增 `getLastStats()` 暴露，并在完成日志里一并打印。
- 新增 `archiveFileSize(parameter)`：按输出类型取 `mbtilesFile` / `pmtilesFile`，文件不存在返回 `0`，
  `IOException` 时打 WARN 并返回 `0`（**不影响任务结果**）。

**使用约定（重要）**

- `getLastStats()` 的注释写明"**一个实例对应一次执行，并发任务请各自新建实例**"——`lastStats` 是实例字段，
  由 `doGenerate` 覆盖写入，多线程共享同一实例会读到彼此的结果。
- 该字段**不是 `transient`**，而 `SparkVectorTileGeneratorV3` 本身 `implements Serializable`，
  序列化时会一并带上上一次的统计值。

**对 V1/V2 的影响**：**仅 V3 生效**（类在 `impl/v3` 包，成员是 V3 生成器新增的）。
调用方仓库 `geoair-mvt-server` 已经接上：`TileGenerationInvoker.invoke(...)` 第 53 行读取
`generator.getLastStats()` 并返回给 `TileTaskServiceImpl`（第 668 行）。

---

### 主题 6：PMTiles 按需读取（V3 自建）

**解决什么问题**：预览侧需要按 `(z, x, y)` 读取归档中的**单个瓦片**，而原先只有写入能力。

**涉及文件**：**新增** `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/impl/v3/V3PmtilesReader.java`（**327 行**）

**关键点**

- `readTile(Path file, int z, int x, int y)`：读 127 字节 Header（校验 `PMTiles` 魔数与版本 3）→ 读根目录 →
  `tileId(z, x, y)` 命中连续段（`runLength > 0`，段内每个瓦片长度相同，按序号偏移定位）或叶子目录入口
  （`runLength == 0`，偏移为 `leafDirectoryOffset + entry.offset`）→ 再进一层查找 → 取数据段 → gzip 则解压。
- `readMetadata(Path file)`：按 Header 里的元数据偏移/长度读取并返回 JSON 文本；不存在时返回 `null`。
- `contains(Path file, int z, int x, int y)`：`readTile` 的布尔封装，异常一律吞掉返回 `false`。
- `tileId(z, x, y)`：与归档**写入端使用同一套** Hilbert 曲线累计编号算法（`base = ((1 << (2z)) - 1) / 3`），
  保证按同一坐标一定能取到同一个瓦片；级别限定 0–29，越界坐标抛 `IllegalArgumentException`。
- 目录段解析：`count → tileId 增量 → runLength → length → offset` 四段 varint（`VarintReader`），
  `offset == 0` 表示不占数据段、非零表示"实际偏移 + 1"；`readDirectory` 先按 gzip 魔数（`0x1f 0x8b`）判断，
  解析失败再兜底解压重试。
- `readSection` 基于 `SeekableByteChannel` 定位读取，读到文件末尾抛 `IOException`（数据不完整）。

**为什么不去改共用的 `PmtilesUtils`**：它只负责"写出归档"，且目录条目等内部结构是 `private`；
更重要的是它属于被归档流程共用的类，V3 不应为了新增读取能力去动它（见第二节）。
本实现只依赖 PMTiles 规范中的公开格式约定。

**当前接线状态**：本仓库与调用方仓库都还没有调用方——调用方 `geoair-mvt-server` 的预览读取目前由它自己的
`PmtilesTileReader` 承担。本类是已落地、待接线的能力。

**对 V1/V2 的影响**：**仅 V3 生效**（新增独立类，且**当前无人调用**，对任何既有链路都是零影响）。

---

### 主题 7：共用代码的两处缺陷修复（有意保留，会改变 V1/V2 产物）

> **这一节是本轮唯一"越界"到共用代码的改动，且是用户看过说明后明确选择保留的。**
> 两处都是**真 bug 修复**，修复后 **V1/V2 重新切片会产出比历史更多的瓦片（即变正确）**，
> 与历史产物不再一致。**这是用户已知悉并接受的结果，不是意外。**

#### 7.1 `TileIterator`：整个缩放级别被静默跳过

**涉及文件**：`geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/utils/TileIterator.java`（**+10 / -6**）

**根因**（修复前的代码）：

```java
if (!initialized) {          // ← 只在进入方法时初始化一次
    initCurrentZoom();
}
while (hasNext) {
    if (currentX >= xMin && currentX <= xMax && ...) { return true; }
    moveToNextTile();
}
```

而 `initCurrentZoom()` 在遇到**空范围**（`xMin > xMax || yMin > yMax`）时会调用 `moveToNextZoom()`：
`currentZoom++` 并 `initialized = false`。由于 `if (!initialized)` 写在 `while` **之前**，
推进后的层级**再也不会重新计算瓦片范围**，`hasNext()` 会沿用上一层残留的 `xMin/xMax/currentX/currentY`
做判断，**整个缩放级别被静默跳过**（表现为"有些网格有内容、有些网格没有内容"）。

**修复**：把初始化判断挪进 `while` 循环体内，并在调用后 `continue`：

```java
while (hasNext) {
    if (currentZoom > maxZoom) { hasNext = false; return false; }
    if (!initialized) { initCurrentZoom(); continue; }   // 空范围推进层级后可反复重新初始化
    if (currentX >= xMin && currentX <= xMax && ...) { return true; }
    moveToNextTile();
}
```

**影响面**：**共用代码，V1 / V2 / V3 都会走到**。`TileIterator` 由
`VectorTileCommonUtils.mapSingleFeatureToTilesStream(...)` 第 143 行构造，调用方为
`impl/v1/V1SparkTaskFunctions.java`（第 210 行）、`impl/v2/V2SparkTaskFunctions.java`（第 143、193 行）、
`impl/v3/V3SparkTaskFunctions.java`（第 46 行）。

#### 7.2 `TileConverter3857Utils`：3857 网格瓦片索引边界修正

**涉及文件**：`geoair-modules/geoair-geo/geoair-geo-tools/src/main/java/cn/geoair/map/dynamic/tools/grid/converter/TileConverter3857Utils.java`（**+45 / -24**）

**修复前的做法**：直接对传入的 `Envelope` 取 `floor` / `ceil` 换算索引，**没有任何兜底与夹取**：

```java
long minX = (long) Math.floor((tileBox.getMinX() - tileOrigin[0]) / width);
long maxX = (long) Math.ceil(((tileBox.getMaxX() - tileOrigin[0]) / width));
long minY = (long) Math.floor((tileOrigin[1] - tileBox.getMaxY()) / height);
long maxY = (long) Math.ceil((tileOrigin[1] - tileBox.getMinY()) / height);
return new RangeApo(minX, maxX, minY, maxY, z);
```

**修复后的做法**（`tileRangeByBox(int z, Envelope box)`）：

1. **退化范围兜底**：`Envelope` 为空、或 X/Y 跨度小于 `POINT_OFFSET`（继承自 `TileConverterCommon`，值 `0.0001`）时，
   以中心点 ± `POINT_OFFSET` 重建一个非退化 `Envelope`，保证点要素、零跨度几何能落进某一个瓦片。
2. **Web Mercator 范围夹取**：四个边界值经新增的 `clampDouble` 夹到 `±MAX_MERCATOR`
   （`MAX_MERCATOR = EARTH_RADIUS * Math.PI`），避免越界坐标算出非法索引。
3. **索引边界修正**：`minX/minX/minY/maxY` 经新增的 `clampLong` 夹到 `[0, 2^z - 1]`
   （`maxTileIndex = (1L << z) - 1`）。原因写在代码注释里：`ceil` 可能得到 `2^z`，
   **不夹取则下游 `xyzToQuadKey` 会抛「X/Y坐标超出范围」**。
4. 结果显式转 `int` 交给 `RangeApo`。注意 `RangeApo` 的四个索引是**闭区间**语义
   （`[minX, maxX] × [minY, maxY]`，类注释明确"最大 X/Y 必须参与遍历"）。
5. 顺带清掉了原先注释掉的大段历史备用计算代码。

**影响面**：**共用代码，V1 / V2 / V3 都会走到**，而且不止切片链路。
`GirGeoTools.getTileGridBingMapOpt()` / `getTileGrid3857Opt()` 统一持有本类（`GirGeoTools.java` 第 151 行），
而所有 `tileRangeByGeom(z, geom)` 调用都会经 `TileConverterCommon.tileRangeByGeom` → `tileRangeByBox`
落到这个被修复的方法上；`zxyListByGeom` 也走同一条路。可见的调用方包括
`vectile/utils/TileUtils.java`（第 29 行，V1/V2/V3 的瓦片范围计算）、`TileIterator`、
`V2SparkTaskFunctions`、`VectorTileCommonUtils`、`MultiLayerMvtEncoderV3`，以及
`geoair-real-mvt` 的 `AbstractITileExecutor` 与瓦片融合 / 预缓存链路（`geoair-map-tile-fuser`）。

#### 7.3 这两处修复的产物影响（显著标注）

> **V1/V2 重新切片后，会产出比历史更多的瓦片。**
>
> - 修复前：退化范围（点、零跨度几何）算不出合法的瓦片范围，`ceil` 还可能得到 `2^z` 的越界索引被下游拒绝，
>   于是**网格边界上的瓦片实际没有被产出**（表现为"少算边界瓦片"、甚至整级被跳过）。
> - 修复后：这些瓦片会被正确算出并夹在法律范围内，因此**重新切片的结果会比历史产物多出这些边界瓦片**。
> - 这是**修复而非行为变更**，但结果确实与历史产物**不再一致**。下游若存在依赖"某些瓦片不存在"的
>   既有数据或判断，需要在重新切片时一并核对。**该结果用户已知悉并接受，不属于意外。**

---

## 四、文件清单

路径均为**仓库相对路径**。增删数字来自 `git diff --numstat` 与 `wc -l` 的真实输出。

### 修改文件（7 个，`699 insertions(+), 94 deletions(-)`）

| 文件路径 | 类型 | 增 / 删 | 一句话说明 |
| --- | --- | --- | --- |
| `geoair-modules/geoair-geo/geoair-geo-tools/src/main/java/cn/geoair/map/dynamic/tools/grid/converter/TileConverter3857Utils.java` | 修改（共用） | **+45 / -24** | 3857 网格边界修正：退化范围兜底、Mercator 夹取、索引夹到 `[0, 2^z-1]`；**V1/V2/V3 都走，产物会变** |
| `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/dto/v3/MultiLayerTileSliceParameter.java` | 修改（仅 V3） | **+14 / -0** | 新增任务级 `buffer`（默认 8）/ `tileSizeOptimizeRounds`（默认 4） |
| `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/dto/v3/MvtLayerSliceParameter.java` | 修改（仅 V3） | **+38 / -2** | 新增 `excludeFields` / `simplifyLines` / `preserveSharedNodes` / `dropSmallestAsNeeded` / `generateIds`，并明确 `coalesceDistance` 语义 |
| `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/impl/v3/MultiLayerMvtEncoderV3.java` | 修改（仅 V3） | **+505 / -31** | 编码器重写：属性裁减、几何降级、点聚合、排序裁剪、超限降级阶梯、空瓦片返回空数组 |
| `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/impl/v3/SparkVectorTileGeneratorV3.java` | 修改（仅 V3） | **+63 / -3** | 所有写出路径跳过空瓦片；新增 `lastStats` / `getLastStats()` / `archiveFileSize()` |
| `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/impl/v3/V3TileFeatureGroup.java` | 修改（仅 V3） | **+24 / -28** | `merge` 改为只做安全阀截断（`capLayerFeatures`），移除聚合期密度裁剪 |
| `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/utils/TileIterator.java` | 修改（共用） | **+10 / -6** | `initCurrentZoom()` 移入 `while` 循环内反复初始化，修复整级瓦片被静默跳过；**V1/V2/V3 都走，产物会变** |

### 新增文件（3 个，共 635 行，均未跟踪 `??`）

| 文件路径 | 类型 | 行数 | 一句话说明 |
| --- | --- | --- | --- |
| `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/impl/v3/V3FeatureUtils.java` | **新增**（仅 V3） | **239** | V3 自建要素工具：gzip、按空间密度合并/丢弃（排序关键字一次性缓存）、编码前按屏幕占用排序；`pipeline(...)` 为预留封装，暂无调用方 |
| `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/impl/v3/V3PmtilesReader.java` | **新增**（仅 V3） | **327** | V3 自建 PMTiles V3 按需读取（Header + 根/叶目录 + varint 段 + gzip）；当前暂无调用方 |
| `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/impl/v3/V3TileWriteStats.java` | **新增**（仅 V3） | **69** | 写出统计值对象（瓦片数 / 字节数 / 批次数 / 读取要素数 / 归档大小），已被调用方仓库消费 |

### 共用类（`git diff` 为空，本轮**无改动**）

| 文件路径 | 状态 |
| --- | --- |
| `geoair-modules/geoair-geo/geoair-mvt/geoair-mvt-tools/src/main/java/cn/geoair/map/dynamic/mvt/tools/AdvMvtDensityUtils.java` | 已回退，**与原版一致** |
| `geoair-modules/geoair-geo/geoair-mvt/geoair-mvt-tools/src/main/java/cn/geoair/map/dynamic/mvt/tools/PipelineBuilder.java` | 已回退，**与原版一致** |
| `geoair-modules/geoair-geo/geoair-mvt/geoair-mvt-tools/src/main/java/cn/geoair/map/dynamic/mvt/tools/model/FeatureWithGrid.java` | 已回退，**与原版一致** |
| `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/impl/v3/archive/PmtilesUtils.java` | 已回退，**与原版一致** |

---

## 五、性能修复专节：`reduceByKey` 中的平方级开销

### 5.1 根因调用链

修复前的调用链（`merge` 是 Spark `reduceByKey` 的合并函数）：

```
V3TileFeatureGroup.merge                       ← 同一瓦片每来一条记录就调用一次（第 N 条即第 N-1 次）
  └─ limitLayerFeatures
       ├─ AdvMvtDensityUtils.doCoalesceBySpatialDensity
       └─ AdvMvtDensityUtils.doFilterBySpatialDensity
            └─ 对要素列表排序
                 └─ Comparator 的次关键字分支
                      └─ getFeatureId(feature, geomFieldName, idFieldName)
                           └─ Geometry.toText()        ← 几何 WKT 序列化
```

四个问题叠加：

1. **`merge` 会被调用 N-1 次**（同一瓦片的第 N 条记录就是第 N-1 次合并）。任何 O(k log k) 的处理放在这里，
   累加起来就是 **O(n² log n)** 量级。
2. **每次调用都含几何序列化**：排序比较器在密度相同、需要按 id 兜底比较时会调用 `getFeatureId`，
   而它在 id 字段取不到值时会退回 `Geometry.toText()`（WKT）。比较器要被调用 O(k log k) 次，
   每次都重新序列化几何，**排序本身就退化成瓶颈**。
3. **网格划分退化**：共用实现用要素**自己的外接矩形**当"瓦片范围"算密度网格，对点要素等于除以零。
4. **每次合并都整列表拷贝**：旧 `limitLayerFeatures` 每次都 `new ArrayList<>(result.subList(0, limit))`。

百万级数据下直接卡死。

### 5.2 修复手法

| 位置 | 修复前 | 修复后 |
| --- | --- | --- |
| `V3TileFeatureGroup.merge` | 调用 `limitLayerFeatures`（含密度裁剪与排序） | 调用 `capLayerFeatures`：**只截断、不排序**，且仅在要素数 > `hardFeatureLimit × 2`（`CAP_TRIGGER_FACTOR = 2`）时才真正截断，把列表拷贝开销摊薄到几乎为零 |
| 要素数限制 / 密度裁剪的位置 | 聚合阶段（`reduceByKey` 内，被调用 N-1 次） | **编码前做一次**：`MultiLayerMvtEncoderV3.applyFeatureLimits` |
| 密度网格的划分依据 | 要素自身外接矩形（点要素退化） | **瓦片范围**（`V3FeatureUtils` 的 `GRID_SCALE_PROJECTED` / `GRID_SCALE_GEOGRAPHIC`） |
| 排序关键字 | 在比较器里**现算** `getFeatureId` → `Geometry.toText()` | **一次性算好并缓存**到 `V3FeatureUtils` 私有静态类 `RowWithCell.sortKey`，比较器只做 int 比较 + 预计算字符串比较 |
| 点聚合 | 分散在编码轮次里 | 提到编码前做一次（`clusterPoints`），后续所有降级轮次复用结果 |

### 5.3 最终落点（重要）

因为**共用的 `AdvMvtDensityUtils` 与 `FeatureWithGrid` 已整体回退**，所以：

- "排序关键字缓存"这件事**最终落在 V3 自己的 `V3FeatureUtils.filterBySpatialDensity` 内部**，
  不在共用类里；共用的 `AdvMvtDensityUtils` 仍然是在比较器里现算。
- "要素数削减下限"这件事落在 **`MultiLayerMvtEncoderV3.resolveTrimFloor`**（`TRIM_FLOOR_RATIO = 0.01`、
  `TRIM_FLOOR_MIN = 200`），共用版本没有下限概念。
- "防 OOM 安全阀"这件事落在 **`V3TileFeatureGroup.capLayerFeatures`**（只截断、不排序、按倍率延迟执行）。

设计取舍写在代码注释里：`merge` 现在**只负责防 executor OOM**；而"真正的要素数限制、密度合并与丢弃
统一放在编码前做一次"。

---

## 六、兼容性与 V1/V2 影响

### 6.1 新增字段都有默认值，不配置时行为不变

| 位置 | 字段 | 默认值 | 与原行为的关系 |
| --- | --- | --- | --- |
| 图层 | `excludeFields` | 空列表 | 不排除任何字段，结果不变 |
| 图层 | `simplifyLines` | `true` | 与原"简化时一律走拓扑保持"相比，线要素仍会被简化，结果不变 |
| 图层 | `preserveSharedNodes` | `true` | 面要素仍走 `TopologyPreservingSimplifier`，与原来一致 |
| 图层 | `coalesceDistance` | `null` | 仅 `> 0` 才触发点聚合，默认不聚合 |
| 图层 | `dropSmallestAsNeeded` | `false` | 超限裁剪仍按数据读取顺序 |
| 图层 | `generateIds` | `false` | 不写 MVT 特征 id，与原来一致 |
| 任务 | `buffer` | `Integer 8` | **与原硬编码 `new VectorTileEncoder(4096, 8, false)` 完全一致** |
| 任务 | `tileSizeOptimizeRounds` | `int 4` | 仅影响超限后的重编码节奏，不改变达标瓦片 |

### 6.2 旧 ticket 是否兼容：**兼容**

- ticket 的形态是 **URL 编码 + Base32 包装的 JSON**，解析入口是
  `MultiLayerTileSliceParameter.fromBase32(String)`（该文件第 149–155 行）：
  `Base32.decodeStr(URLUtil.decode(baseString))` → `JSON.parseObject(json, MultiLayerTileSliceParameter.class)`；
  调用方是 `geoair-mvt-server` 的 `TileGenerationInvoker.invoke(parameterTicket, ...)`（第 44 行）。
- **`dto/v3` 包内没有任何 Jackson 注解**：已逐个核对，`@JsonProperty` / `@JsonIgnore` / `@JsonAlias` /
  `@JsonInclude` **一个都没有**，字段序列化完全走 JSON 字段名映射。
- 本轮新增字段是**纯追加**，既有字段**一名未改、语义未变**（`simplificationLevel` / `coalesceDistance`
  只改了注释）。旧 ticket 的 JSON 里**缺少**这些新字段，反序列化后字段**保持字段初始值**（即上表的默认值），
  不会抛异常、也不会变成 `null`。
- 反向：用新版代码生成的 ticket 被旧版代码读取时，多出的字段属于未知字段，按 hutool `JSON.parseObject`
  的默认行为被忽略（该行为未被本仓库改写），不会因此报错。
- `buffer` 是对象类型 `Integer`，编码器里另有 `parameter.getBuffer() == null ? DEFAULT_BUFFER : Math.max(0, ...)`
  的二次兜底，**即使被显式置为 `null` 也能正常工作**。

**结论：旧 ticket 可直接继续使用，无需重新生成。**

### 6.3 空瓦片语义变化对下游的影响

- **统计**：瓦片数不再虚高，更接近真实覆盖范围；同一份数据重新切片后，**瓦片数会比历史结果少**
  （减少的是原先 `tile_data` 为空的记录），对比新旧统计时不要误判为数据丢失。
- **预览**：预览端依赖"瓦片是否存在"做判断，空瓦片不再被当作有效瓦片返回，此前可能出现的空白瓦片渲染问题会消失。
- **PostgreSQL**：跳过空瓦片意味着该 tileId 的历史记录**不会被本次任务的 `delete` 清掉**；同表重复切片需确认符合预期。
- **本地目录 / S3 / 归档**：原先"编码结果为空"会抛 `IllegalStateException` 中断任务，现在静默跳过；
  原先靠这个异常发现问题的场景需改用日志观察。

### 6.4 `V3TileWriteStats` 的使用约定

- `lastStats` 是**实例字段且非 `transient`**，所在类 `implements Serializable`；一个实例对应一次执行，
  并发任务请各自新建实例。
- 该对象已被调用方仓库消费（`TileGenerationInvoker` → `TileTaskServiceImpl`），字段语义见主题 5。

### 6.5 构建要求

- 本轮改动涉及的模块：`geoair-geo-tools`（`TileConverter3857Utils`）与 `geoair-static-mvt-spark`
  （V3 相关 6 个文件 + `TileIterator`）。
- **`geoair-mvt-tools` 本轮无改动**（已回退），因此不再因为共用类而需要重新发布该模块。
- 但上述两个模块是上游产物，**需要重新 `mvn install`（或整体构建）**，否则调用方
  （如 `geoair-mvt-server`）仍会用到旧 class：`V3TileWriteStats` 是**新类**，旧 class 会直接
  `NoClassDefFoundError`；`TileIterator` 与 `TileConverter3857Utils` 的修复也不会生效。

### 6.6 V1/V2 影响面速查

| 改动 | 生效范围 | V1/V2 产物是否变化 |
| --- | --- | --- |
| V3 两个 DTO 新增字段（主题 1） | 仅 V3 | 否（全部有默认值） |
| `MultiLayerMvtEncoderV3` 重写（主题 2） | 仅 V3 | 否（默认参数下行为不变；仅显式开启新参数或瓦片超限时才走降级链） |
| `V3TileFeatureGroup` 聚合期改造（主题 3） | 仅 V3 | 否（要素数限制在编码前仍会做一次） |
| `SparkVectorTileGeneratorV3` 空瓦片跳过 + 统计（主题 4、5） | 仅 V3 | 是（**V3** 瓦片数不再含空记录） |
| `V3FeatureUtils`（新增，主题 2、3） | 仅 V3 | 否 |
| `V3PmtilesReader`（新增，主题 6，当前无调用方） | 仅 V3 | 否 |
| `V3TileWriteStats`（新增，主题 5） | 仅 V3 | 否 |
| **`TileIterator` 初始化修复（主题 7.1）** | **共用代码，V1 / V2 / V3 都走** | **是（原先被静默跳过的整级瓦片恢复产出，比历史更多）** |
| **`TileConverter3857Utils` 边界修正（主题 7.2）** | **共用代码，V1 / V2 / V3 及实时切片、融合/预缓存链路都走** | **是（原先缺失的边界瓦片恢复产出，比历史更多）** |
| 4 个共用类（`AdvMvtDensityUtils` / `PipelineBuilder` / `FeatureWithGrid` / `PmtilesUtils`） | **本轮无改动** | 否（`git diff` 为空，见第七节） |

**对"修改 geoair 不要影响原来 V1 和 V2 的逻辑"这条要求的客观回答**：

- **V3 专属的 7 项改动完全不影响 V1/V2**（V3 两个 DTO、编码器、生成器、`V3TileFeatureGroup`、
  `V3FeatureUtils`、`V3PmtilesReader`、`V3TileWriteStats`）。
- 共用代码只有 **2 项**改动，且都是**缺陷修复**：`TileIterator` 与 `TileConverter3857Utils`。
  它们会让原先错误缺失的瓦片重新产出，因此 **V1/V2 重新切片后的产物会与历史产物存在差异**。
  这是用户**看过说明后明确选择保留**的结果，已知悉并接受。

---

## 七、状态说明

**本工作区改动全部未提交、未暂存。**

- **当前分支**：`feature/J8-dev`
- **仓库根目录**：`H:\gitee\geoair-root\geoair-framework`
- **基线提交**：`2c670e4913de483778f7be4b0b2453827b5fa7aa`
- **`git status --short` 共 11 行**：7 个未暂存修改（` M`）+ 4 个未跟踪（`??`：3 个新增 `.java` + 本文档自身）
- 本轮**没有执行任何 git 写操作**（未 `add` / `commit` / `stash` / `checkout`），也未修改除本文档之外的任何文件。

### 共用类 `git diff` 为空的确认（已亲自核对）

核对方式：对每个路径执行 `git diff --stat -- <path>`（无输出）、`git diff -- <path> | wc -c`（**0 字节**），
并用 `git hash-object <path>` 与 `git rev-parse HEAD:<path>` 比对 blob 哈希——**两者完全相同**：

| 文件路径 | `git diff` 字节数 | 工作区 blob 哈希 = `HEAD` blob 哈希 |
| --- | --- | --- |
| `geoair-modules/geoair-geo/geoair-mvt/geoair-mvt-tools/src/main/java/cn/geoair/map/dynamic/mvt/tools/AdvMvtDensityUtils.java` | **0** | `1c4f5dc271afdbd148b9ca1de3e365d09ef0a715` |
| `geoair-modules/geoair-geo/geoair-mvt/geoair-mvt-tools/src/main/java/cn/geoair/map/dynamic/mvt/tools/PipelineBuilder.java` | **0** | `b4cf65ee1b1abb2e222abd861cbe154f11aeb28e` |
| `geoair-modules/geoair-geo/geoair-mvt/geoair-mvt-tools/src/main/java/cn/geoair/map/dynamic/mvt/tools/model/FeatureWithGrid.java` | **0** | `e056fa80597716174119b6fecd72476b90305903` |
| `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/src/main/java/cn/geoair/map/dynamic/statics/mvt/spark/vectile/impl/v3/archive/PmtilesUtils.java` | **0** | `ae31d708c0cf8b835ff5ca91a5909e2f96f795d6` |

**结论：这 4 个共用类均与 `HEAD` 版本逐字节一致，"V3 不改共用代码"这条设计约束已落实到位。**

如需提交，请先确认第六节 6.6 表中标记为"**是**"的两项共用缺陷修复（`TileIterator`、
`TileConverter3857Utils`）符合预期——它们会让 V1/V2 重新切片后产出更多（正确）的瓦片。
