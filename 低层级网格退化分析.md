# 低层级瓦片网格退化分析

> 调查对象：`geoair-geo-tools` 里的瓦片网格换算
> 起因：调用方（`geoair-mvt-server`）用 `MIN_VALID_ZOOM = 6` 挡住了 z<6，理由是"两套网格在低层级会退化"。
> 用户判断这是 bug，要的是把低层级算对，不是继续用限制回避。
> 本文只写实测能证实的东西，证不了的单独列在最后一节。

---

## 零、已实施（2026-09-19）

按 6.1 的对照结论选了"±90 语义"。改动落在 `geoair-geo-tools`，提交 `11bb8c84`：

| 改动 | 说明 |
|---|---|
| `GirGeoTools.getTileGrid4326Opt()` | 改为返回 `Wgs84SeparateAxisTileUtils`；两条 4326 入口现在指向同一套网格，对外方法签名不变 |
| `Wgs84EqualAxisTileUtils` | 标 `@Deprecated`，删掉全部重写、改为继承 Separate —— 既有引用照常编译，行为随之修正 |
| `AbstractWgs84TileConverter` | 删掉只服务于 ±85.05 裁剪的 `MAX_VALID_LAT` / `MIN_VALID_LAT` |
| 调用方 `geoair-mvt-server` | `MIN_VALID_ZOOM` 由 6 改 0，低层级预览放开（提交 `dfb9571`） |

验证结果：

- 判据探针对照 `EPSG:4326_19` / `EPSG:3857_19`，z=0..12 的列数、行数、首末行纬度、全域反算区间、各纬度落点 **103 项全过**
- 两条 4326 入口逐级逐瓦片比对，输出**逐行一致**
- 3857 网格本次未动，逐级自洽
- `geoair-geo-tools` 单测 `TileRangeTest` 5 项通过
- 端到端（8099 + 本地库）：GeoJSON 数据集在 4490 与 3857 下 z=0..6 均出瓦片；反证不含数据的瓦片仍返回 0 字节

**还没做的是 V1/V2/V3 的切片产物回归**（见第八节），这是本节唯一留口。

---

## 一、先说结论

1. **问题出在 4326 的等轴实现 `Wgs84EqualAxisTileUtils` 上，而且不只 z<6 —— 每一级都有。**
   它的 `tileRangeByBox` 用 `(1<<z)-1` 当 y 的索引上限，而该网格的真实行数只有 `2^(z-1)`。
   结果是从 z=1 开始，反算就会产出**不存在的行号**，这些行号换回经纬度是**零高度的空范围**。

2. **3857 实现（`TileConverter3857Utils`）实测正常**，z=0..8 的首尾瓦片范围、全域反算全部自洽。

3. **4326 非等轴实现（`Wgs84SeparateAxisTileUtils`）实测也正常**，用的正是正确的行数写法。
   这里更正一个我一开始的判断：它**并非没人用**，生产里有 4 个调用点（见 3.4），
   但**用途全是 XYZ↔TMS 行号换算**，而这类换算只取"行数"，行数由父类统一算出、两套完全相同
   —— 换成哪个实例结果都一样，等于没用到它的特性。
   **真正决定"哪些瓦片要切"的范围换算，生产里 100% 走等轴那套。**

4. 所以"z<6 才退化"这个说法，**我这一轮没能复现出证据**。实测看，等轴网格在每一级都有 50% 的 y 是无效行（z=1 时 1 个真实的 + 1 个不存在的，z=5 时 16 个真实的 + 16 个不存在的），跟层级高低没有本质关系。
   为什么当初会得出"卡在 6"的结论，需要对上当时的实验记录（见第五节）。

---

## 二、复现方式

探针：`E:\temp\gridprobe\GridProbe.java`（**不在仓库里**，独立 main，不改任何生产代码）
依赖：`geoair-mvt-server` 部署目录的 `lib/*`（其中的 `geoair-geo-tools` 已用 `javap` 核对过，含 `clampLong`/`clampDouble`，与当前 `feature/J8-dev` 源码一致）

三套网格各跑 z=0..8，每级检查四件事：

- 元数据行列数（`getTileLevelMetadata`）
- 首瓦片 `(0,0)` 与末瓦片 `(cols-1, rows-1)` 的范围
- **全域反算**：把首尾瓦片合起来的大范围丢回 `tileRangeByBox`，看是否回到 `[0, cols-1] × [0, rows-1]`
- **往返一致性**：取中间某瓦片，`xyzToTileBox` 拿到范围后再反算，看是否回到同一格
- 逐行的纬度范围（看有没有零跨度的行），以及若干测试点（含南北半球、极区）的归属

---

## 三、实测数据

### 3.1 等轴网格（`getTileGrid4326Opt()`，生产在用）

元数据本身是自洽的：列数 `2^z`、行数 `2^(z-1)`（`AbstractWgs84TileConverter.getTileLevelMetadata` 第 155 行 `tileHeight = max(1, tileWidth / 2)`）。
问题出在反算那一步。

| z | 列数 | 真实行数 | 反算允许的 y 上限 | 不存在的行数 | 全域反算结果 |
|---|---|---|---|---|---|
| 0 | 1 | 1 | 0 | 0 | `x[0..0] y[0..0]` OK |
| 1 | 2 | 1 | 1 | **1** | `x[0..1] y[0..1]` ← y=1 不存在 |
| 2 | 4 | 2 | 3 | **2** | `x[0..3] y[0..2]` ← y=2 不存在 |
| 3 | 8 | 4 | 7 | **4** | 同上，y 上限=7 |
| 4 | 16 | 8 | 15 | **8** | 同上 |
| 5 | 32 | 16 | 31 | **16** | 同上 |

那些"不存在的行"换回经纬度是什么样（z=2，行数 2，非法 y=2、3）：

```
非法行 y=2 的纬度范围 = [-85.0511, -85.0511]   跨度 = 0.0000   ← 退化
非法行 y=3 的纬度范围 = [-85.0511, -85.0511]   跨度 = 0.0000   ← 退化
```

测试点的归属（z=2）：

```
点( 26.74°N) -> x[3..3] y[0..1]
点(-26.74°N) -> x[3..3] y[1..2]     ← y=2 不存在
点(  0.00°N) -> x[3..3] y[0..2]     ← y=2 不存在
```

### 3.2 3857 网格（`getTileGrid3857Opt()`）

z=0..8 全部正常：

```
--- z=5  cols=32 rows=32
    首瓦片(0,0)  = [-20037508.3428, -18785164.0714] x [18785164.0714, 20037508.3428]
    末瓦片(31,31)= [18785164.0714, 20037508.3428] x [-20037508.3428, -18785164.0714]
    全域反算      -> x[0..31] y[0..31]   OK
    往返 (x=16,y=16) -> x[15..17] y[15..17]   ← 多算一格
```

唯一不干净的地方是**往返多算一格**：`tileXmax = ceil(...)` 之后没有减 1（注释里写明闭区间语义），再叠加浮点误差让 `floor` 在格线上向下偏 1，于是从"正好落在一格内"变成"覆盖 3 格"。
方向是**多算不漏**，不影响正确性，只是浪费。

### 3.3 非等轴网格（`getTileGrid4326SeparateOpt()`）

z=0..2 全对：

```
--- z=2  cols=4 rows=2
    首瓦片(0,0) = [-180, -90] x [0, 90]
    末瓦片(3,1) = [90, 180] x [-90, 0]
    全域反算     -> x[0..3] y[0..1]   OK      ← y 上限正确地用 1
    点(-26.74°N) -> x[3..3] y[1..1]
```

### 3.4 谁在用哪套（生产调用点清单）

**等轴（`getTileGrid4326Opt()`）—— 范围换算全部落在这里**：

| 调用点 | 位置 | 干什么 |
|---|---|---|
| 静态切片 V1/V2/V3 | `geoair-static-mvt-spark/.../vectile/utils/TileUtils.java:31,42` | `tileRangeByGeom` 算要素落哪些瓦片；`xyzToTileBox` 反查瓦片范围 |
| 实时切片 | `geoair-mvt-tools/.../AdvMvtTileUtils.java:31`（`getTileRect`） | 按 z/x/y 算瓦片地理范围 |
| 实时切片 servlet | `geoair-real-mvt/.../servlet/VectorTileV2Servlet.java:125` | 同上 |
| 瓦片融合 / 预缓存 | `geoair-map-tile-fuser/.../precache/TileTaskExecutor.java`（6 处）、`TileServiceTran.java:337` | 范围换算与反查 |
| 瓦片对象 | `geoair-geo-tools/.../grid/dto/TileZxyApo.java:159` | 由 z/x/y 得到范围 |
| 管理端 | `geoair-mvt-server` 的 `GeoJsonMvtTileService`（经 `AdvMvtTileUtils`） | 数据集预览的实时切块 |

**非等轴（`getTileGrid4326SeparateOpt()`）—— 只用于行号换算**：

| 调用点 | 位置 | 用途 |
|---|---|---|
| V3 生成器 | `SparkVectorTileGeneratorV3.getTmsY`（328 行） | XYZ → TMS 行号 |
| V1 / V2 | `V1SparkTaskFunctions.java:331`、`V2SparkTaskFunctions.java:326` | 同上 |
| 融合缓存 | `geoair-map-tile-fuser/.../utils/FuserCacheUtils.java:118`（`reverseY`） | 同上 |

这几处调的都是 `convertY(...)`，内部走 `getTileRowCount(z)` → `getTileLevelMetadata(z).getNumTilesHigh()`。
**行数由父类 `AbstractWgs84TileConverter` 统一算（`2^(z-1)`），两套实现拿到的是同一个数**，所以这些调用换成等轴实例结果完全一样。

**一句话**：范围换算认等轴，行号换算两套等价。

### 3.5 两套实现的逐项差异（代码级）

先说结论：**叫法上的"等轴 / 非等轴"，在当前代码里已经不成立了** —— 两套的跨度公式、行列数完全一样，实际差异全部集中在"纬度覆盖到哪"。

| 项目 | 等轴 `Wgs84EqualAxisTileUtils` | 非等轴 `Wgs84SeparateAxisTileUtils` | 是否相同 |
|---|---|---|---|
| 经度跨度 `calculateTileLonSpan` | `360/2^z` | `360/2^z` | **相同** |
| 纬度跨度 `calculateTileLatSpan` | `360/2^z` | `360/2^z` | **相同** |
| 行列数（父类 `getTileLevelMetadata`） | `2^z` 列 × `2^(z-1)` 行 | 同左 | **相同** |
| `xyzToTileBox` 纬度裁剪（66-69 / 73-77 行） | `±85.0511287798` | `±90` | **不同** |
| `tileRangeByBox` 纬度裁剪（103-104 / 109-112 行） | `±85.0511287798` | `±90` | **不同** |
| `tileRangeByBox` 的 y 上限（108 / 117-118 行） | `(1<<z)-1`（与 x 共用） | `getNumTilesHigh()-1` | **不同（这处是 bug）** |
| `tileYToCoordinateY` 裁剪（137 / 146 行） | `±85.0511287798` | `±90` | **不同** |
| 行号校验（`convertXxxAxisY`） | 直接返回，不校验 | 走父类，校验 y 是否越界 | 不同（影响小） |

把上表按"行为"翻译一遍，**唯一有实质意义的差别是：纬度覆盖 ±85.05° 还是 ±90°**。

具体到瓦片形状（z=2，经度跨度都是 90°）：

```
等轴    y=0 -> lat [0, 85.0511]      跨度 85.05°   ← 被裁掉 4.95°
        y=1 -> lat [-85.0511, 0]     跨度 85.05°
        → 90° × 85.05°，长方形

非等轴  y=0 -> lat [0, 90]           跨度 90°
        y=1 -> lat [-90, 0]          跨度 90°
        → 90° × 90°，正方形
```

行数少的时候首末两行被压得最明显；层级高了只有第 0 行和最后一行被裁，中间行不受影响（z=5 时 16 行里只有 y=0、y=15 是 6.3° 跨度，其余都是 11.25°）。

`±85.05` 这个值的来历是 Web Mercator 的有效纬度上限（`MAX_VALID_LAT = 85.0511287798`），父类的类注释里也点明了：这个常量只适合"等轴网格为兼容 Web Mercator 所做的纬度裁剪"，非等轴应当覆盖完整 `[-90, 90]`。
所以按代码自己的说法，**等轴的存在意义就是"跟 3857 对齐纬度范围"**（这样同一张地图上切换网格时，视野里的纬度范围一致），而不是"格子是正方形"。

**所以"等轴"这个名字容易误解**：按"经纬跨度相同 → 正方形格子"来理解，反而是非等轴（±90）那套符合。等轴那套因为把纬度裁到 ±85.05，格子在纬度方向被压扁了。

### 3.6 Y 轴翻转：两套实测完全一致

专门跑了一个探针（`E:\temp\gridprobe\YProbe.java`）核对 Y 轴相关行为，结论是**翻转逻辑零差异**。

原因是实现位置决定的：翻转根本不在两个子类里 —— 它全在接口 `GirTileConverterOpt` 的 default 方法里
（`toXyzY` 383 行、`convertY` 136 行、`reverseY`、`toXyzTiles` 422 行、`fromXyzRange` 398 行），
最终落到 `TileYAxis.convertY`：

```java
return this == target ? y : tileRowCount - 1 - y;   // XYZ <-> TMS
```

而那个 `tileRowCount` 来自 `getTileRowCount(z)` → `getTileLevelMetadata(z).getNumTilesHigh()`，
**由父类统一算（`2^(z-1)`），两套拿到的是同一个数**。两个子类都没有 override 这些方法。

实测（z=0..6）：

| 检查项 | 等轴 | 非等轴 | 结果 |
|---|---|---|---|
| 行数 | 1,1,2,4,8,16,32 | 同左 | 一致 |
| XYZ→TMS 映射（逐级逐个 y 比对） | `0` / `0` / `1,0` / `3,2,1,0` / … | 同左 | **逐字节一致** |
| 边界索引 `convertBoundaryY`（`rowCount-y`） | `1,0` / `2,1,0` / `4,3,2,1,0` | 同左 | 一致 |
| 轴间映射 `convertSeparateAxisYToEqualAxisY` | 返回原值 | 返回原值 | 一致 |
| 对越界 y 的处理 | 抛异常 | 抛异常 | 一致 |

（更正一下：我先前说"等轴不校验 Y 越界"，那是看错了 —— 带 override 不校验的是 **3857** 那个类，
两个 4326 子类都走父类、都做校验。）

**两套唯一会给出不同数值的地方，是"行号换成纬度"这一步**（`tileYToCoordinateY`），
而且只差在最北一行：

```
z=3 (4 行)
  y=0  equal=85.0511  separate=90.0000   <<<< 不同
  y=1  equal=45.0000  separate=45.0000
  y=2  equal=0.0000   separate=0.0000
```

同理 `xyzToTileBox` 里首行的 `lat_max`（85.0511 vs 90）与末行的 `lat_min`（-85.0511 vs -90）也不同。

**一句话**：翻转算法一模一样，差异只在"翻转完算纬度"时的裁剪范围 —— 也就是 3.5 节那一处。

---

## 四、根因

### 4.1 y 索引上限用错了常量（主因）

`Wgs84EqualAxisTileUtils.tileRangeByBox` 第 108 行：

```java
int maxTileIndex = (1 << z) - 1;   // ← 这个值同时用于 x 和 y
...
tileXmin = clamp(tileXmin, 0, maxTileIndex);
tileXmax = clamp(tileXmax, 0, maxTileIndex);
tileYmin = clamp(tileYmin, 0, maxTileIndex);
tileYmax = clamp(tileYmax, 0, maxTileIndex);
```

它对 x 是对的（列数确实是 `2^z`），对 y 是错的 —— 该网格行数只有 `2^(z-1)`。

对照：**非等轴实现用的是正确的写法**（`Wgs84SeparateAxisTileUtils` 第 117-118 行）：

```java
int maxTileXIndex = (1 << z) - 1;
int maxTileYIndex = getTileLevelMetadata(z).getNumTilesHigh() - 1;   // ← 按真实行数
```

同一份代码里，两个子类对同一件事给出了两种写法，其中一个明显是对的。

### 4.2 纬度被 Web Mercator 的 ±85.05 覆盖（次因）

`Wgs84EqualAxisTileUtils` 在三处把纬度 clamp 到 `MIN_VALID_LAT / MAX_VALID_LAT`（±85.0511287798，这俩常量来自 Web Mercator 的有效纬度）：

- `xyzToTileBox` 第 68-69 行
- `tileRangeByBox` 第 103-104 行
- `tileYToCoordinateY` 第 137 行

而 `AbstractWgs84TileConverter` 的类注释专门说了：`MAX_VALID_LAT` 只适合等轴网格为兼容 Web Mercator 做的裁剪，非等轴网格应该覆盖完整的 `[-90°, 90°]`。
也就是说这个 clamp 是**有意的**，目的是让等轴网格与 Web Mercator 的可见纬度对齐（地图本身到不了 ±90）。

但它的副作用是：**|lat| > 85.05 的数据找不到能覆盖它的合法瓦片**。
z=2 时纬度 85.05~90 的点算出来 y=0（范围 `[0, 85.05]`，并不包含它）；南纬同理，会被推到那个不存在的 y=2 上。

业务数据里如果不会有 |lat| > 85 的东西，这条可以先不动；但它与 4.1 是叠加在一起放大影响的。

### 4.3 为什么低层级更"显眼"

行数按 `2^(z-1)` 走，非法行的比例恒定是 50%，但**绝对行数少的时候，剩下那点合法行覆盖的纬度太粗**：
z=2 只有 2 行合法，一行吃 90° 纬度（再被 clamp 到 85.05）；z=5 有 16 行合法，一行 11.25°。
调用方如果按"算出几个 y"来做判断，低层级下无效行占比高、结果更不对劲，很容易得出"低层级坏了"的印象。

---

## 五、与原始观察对不上的地方（需要你确认）

这几条我查不出来，如实列着：

1. **"4326 网格 z=5 时 y 只有 0/1 行"** —— 实测 z=5 等轴的行数是 16，y=0 与 y=1 是正常的两行。没复现出"只有 0/1 行"。
   如果你的实验是拿某段具体代码跑的，把那段贴出来我能对上。

2. **"纬度范围会算到 -11.25°~0°"** —— 这个数值本身**是对的**：z=5 时纬度跨度 11.25°，y=8 覆盖 `[-11.25°, 0°]`，那是南半球的正常一行（y 的合法范围是 0..15）。
   如果你的意思是"这一行不该出现"，那结论会不一样，得看当时的判定标准。

3. **"3857 网格 z=5 时 x=47 换算出来 3.88e7"** —— `tileXToCoordinateX(47, 5)` 确实得出 3.88e7，但 z=5 的合法 x 是 0..31，**47 本身就越界了**。
   `validateXyz` 有意不校验 `x < 2^z`（注释说明是为了让 `x+1` 参与右边界计算），所以越界索引不会被拦下，直接算出界外值。
   这更像"当时的实验用错了层级的索引"，而不是网格公式错。如果你希望**上游能拦住越界索引**，那是另一个改动点（可以在 `tileRangeByBox`/`tileXToCoordinateX` 里加上界校验）。

4. **"为什么卡在 6"** —— 我实测不出 z<6 与 z>=6 有本质差别（等轴每级都是 50% 非法行）。
   如果当时是因为某个具体现象（比如地图拖到 z5 白屏）才定在 6，得有那次现象的记录才能对齐。

---

## 六、修法建议与影响面

### 6.1 先定对错：跟底图的矩阵集对照

"哪套对"不该凭偏好定 —— 管理端底图就是按 `{矩阵集}/{z}/{x}/{y}` 取的瓦片，
直接查它的 `GetCapabilities` 最权威。`EPSG:4326_19`（以及 `_17`、`_18`）的定义是：

| z | TopLeftCorner | MatrixWidth | MatrixHeight |
|---|---|---|---|
| 0 | `90 -180` | 1 | 1 |
| 1 | `90 -180` | 2 | 1 |
| 2 | `90 -180` | 4 | 2 |
| 3 | `90 -180` | 8 | 4 |
| 5 | `90 -180` | 32 | 16 |
| 12 | `90 -180` | 4096 | 2048 |

也就是 **`2^z` 列 × `2^(z-1)` 行、纬度起点 90、覆盖 ±90、每级翻倍** —— 标准 GeoWebCache 的 EPSG:4326 网格。

逐项对照：

| | 底图 `EPSG:4326_19` | 非等轴（±90） | 等轴（±85.05） |
|---|---|---|---|
| 列数 | `2^z` | `2^z` ✓ | `2^z` ✓ |
| 行数 | `2^(z-1)` | `2^(z-1)` ✓ | `2^(z-1)` ✓ |
| 纬度起点 | 90 | 90 ✓ | 85.0511 **✗** |
| 纬度覆盖 | ±90 | ±90 ✓ | ±85.05 **✗** |

**结论：非等轴那套与底图矩阵集逐项吻合；等轴那套的 ±85.05 找不到对应依据**
（`AbstractWgs84TileConverter` 的注释自己也说了那只是"为兼容 Web Mercator 所作的纬度裁剪"）。

顺带核对 3857：底图 `EPSG:3857_19` 的 TopLeftCorner 是 `-20037508.342789244 20037508.342789244`，
与 geoair 的 `MAX_MERCATOR = 6378137 * π` 完全一致 —— 3857 那条链路本来就没错，这也解释了 3.2 节它实测干净。

**用等轴的直接后果**：切瓦片时按 ±85.05 归一化要素坐标，前端却按 ±90 解释同一个瓦片，
两者比例不同 → 要素在纬度方向被拉伸。z=0 时底图每像素 `180/256 = 0.703°`、
等轴按 `170.1/256 = 0.664°` 归一化，偏差约 5.5%；层级高了以后受影响的主要是最北、最南两行
（z=5 时那两行的内容会被拉伸约 1.8 倍）。

### 6.2 主因怎么修

`Wgs84EqualAxisTileUtils.tileRangeByBox` 的 y 上限改成按真实行数取，也就是对齐非等轴那套写法：

```java
int maxTileXIndex = (1 << z) - 1;
int maxTileYIndex = getTileRowCount(z) - 1;    // 或 getTileLevelMetadata(z).getNumTilesHigh() - 1
```

一行改动，改完 y 就不会再产出不存在的行。

（实际落地时没单独改这一行，而是按 6.4 走了合并：`Wgs84EqualAxisTileUtils` 整体废弃、改为继承
`Wgs84SeparateAxisTileUtils`，y 上限自然跟着用上正确写法。见第零节。）

### 6.3 影响面（这条必须评估，不能拍脑袋）

这些类是**共用代码**，走它们的不止一处：

| 调用方 | 位置 | 影响 |
|---|---|---|
| 静态切片 V1/V2/V3 的瓦片范围计算 | `geoair-static-mvt-spark` 的 `TileUtils.rangeToIndex` | 4326 网格下算出的 y 集合会变小（去掉无效行） |
| 实时切片的地块范围 | `geoair-real-mvt` 的 `AdvMvtTileUtils.getTileRect` | 同上，4326/4490 预览 |
| 瓦片对象自身 | `TileZxyApo` 第 159 行 | 同上 |
| 瓦片融合/预缓存 | `geoair-map-tile-fuser` 的 `TileTaskExecutor` 第 204 行 | 同上 |

**改动方向有两层**：一是 y 不再产出本来就不存在的行（少算，不会是"漏切"）；
二是首末行的纬度范围从 ±85.05 变成 ±90，落在最北/最南那两行的要素几何在瓦片内的归一化结果会变。
两者叠加，4326 网格的既有产物会有差异，**这是"修对"的内容，但必须跑一次回归确认差异只落在预期的那两行**（见第八节）。

按 `V4期望.md` 第五节的约束，这类改动属于"缺陷修复"性质，是可以做的，但要逐项列出影响（上表就是）。

### 6.4 次因（±85.05）怎么处理

按 6.1 的对照，这个 clamp 是**该去掉的** —— 它让 4326 网格与底图矩阵集对不上。
但去掉就是 3.5 节说的"选 ±90 语义"，会改变现有 4326 产出的首末行范围，所以要和 6.2 一起评估、一起回归，
不能单独顺手删。

保留它的唯一理由是"与 3857 的可见纬度对齐、切网格时视野不跳"，
但那属于**渲染端**的取景问题，不该由瓦片网格定义来承担 —— 网格应该跟底图矩阵集一致。

---

## 七、顺手记一笔：往返多算一格

三套网格都有：`tileXmax = ceil((envMaxX + PRECISION - origin) / width)`，`ceil` 之后不减 1（闭区间语义），再叠加浮点误差让 `floor` 在格线上偏小，于是"取一格的范围再反算"会得到 3 格。

对 3857 的实测（z=5）：

```
往返 (x=16,y=16) -> x[15..17] y[15..17]
```

方向是**多算不漏**，不影响正确性，但如果哪天要按"瓦片集合精确去重"做校验，得知道这里有这么一层放大。

---

## 八、还没做的事

- **V1/V2/V3 的切片回归没跑** —— 这是本次改动剩下的唯一缺口。要确认的只有一件事：
  4326 网格产物的差异是否**只落在最北/最南那两行**，其余层级与格子不变。
  3857 不受影响，可以不动。
- 第五节那四条待对齐（当时实验用的索引/判定标准）。
- 第七节那个"往返多算一格"没动 —— 方向是多算不漏，且改动它会同时影响 3857，风险大于收益。
