# V4 开发计划

> 配套文档：`V4期望.md`（需求/期望）、`V3切片改动说明.md`（V3 本轮改动）、`低层级网格退化分析.md`（网格取证）。
>
> 本文档是**开发计划**，不是需求文档。第一部分先给架构判定与选型依据，第二部分是本轮（v1）的实际范围，
> 最后一部分是实施记录与后续 roadmap —— 实施记录在动手之后回填，不预写。

---

## 一、目标与判定依据

**目标**：做一个**不依赖 Spark 运行时**的静态矢量瓦片生成器 V4，完整保留 V3 的输入/输出/参数能力，
并在其上补 tippecanoe 的能力；同时**不能影响 V1/V2 的任何行为**。

### 1.1 V3 的 Spark 耦合面（逐文件核实，不是估算）

`geoair-static-mvt-spark` 共 **62 个 Java 文件**，其中 `import org.apache.spark.*` 的只有 **18 个**。
剩下的 44 个文件**没有任何 Spark 依赖**，也就是说它们是纯 Java 实现，V4 可以直接复用。

**必须自己重写（18 个）**：

| 文件 | 行数 | 为什么必须自己写 |
| --- | --- | --- |
| `vectile/impl/v3/SparkVectorTileGeneratorV3.java` | 514 | 驱动本身：RDD 编排、`reduceByKey`、`foreachPartition` |
| `vectile/impl/v3/V3ProgressTracker.java` | 119 | 进度靠 Spark `LongAccumulator` 与 Stage 回调 |
| `vectile/impl/v3/V3SparkTaskFunctions.java` | 103 | `PairFlatMapFunction` / `Function2` 函数对象 |
| `vectile/impl/v3/input/V3FeatureReader.java` | 24 | 接口签名就是 `read(SparkSession, layer) → JavaRDD` |
| `vectile/impl/v3/input/V3GeoJsonFeatureReader.java` | 382 | 走 `binaryFiles` / `textFile` 两个 RDD |
| `vectile/impl/v3/input/V3JdbcFeatureReader.java` | 149 | 走 Spark JDBC 分区与 `mapPartitions` |
| `vectile/statistics/StatisticUtils.java` | 473 | 仅 V1/V2 使用，V4 不需要 |
| 其余（V1/V2 驱动、listener、app 入口） | — | 与 V4 无关 |

**可直接复用（关键的那几个）**：

| 能力 | 复用对象 | 说明 |
| --- | --- | --- |
| **PBF 编码 + 全部超限降级阶梯** | `impl/v3/MultiLayerMvtEncoderV3.encode(tileId, group, parameter)` | 这是整个工程最重的一块，**一行不改地复用** |
| 要素按瓦片分配 | `vectile/utils/VectorTileCommonUtils.mapSingleFeatureToTilesStream(...)` | 瓦片键（Bing QuadKey）与 V3 完全同源 |
| 要素坐标转换 | `VectorTileCommonUtils.transformSingleFeature(row, parameter)` | 同上 |
| 瓦片聚合单元与合并 | `impl/v3/V3TileFeatureGroup.single/merge` | `implements Serializable`，可溢写 |
| 密度/身份抽样等工具 | `impl/v3/V3FeatureUtils` | |
| 本地目录 / S3 写出 | `impl/v3/output/V3TileStoreFactory` | 工厂按 `outputType` 分发 |
| MBTiles / PMTiles 归档 | `impl/v3/archive/MbtilesArchiveUtils`、`PmtilesUtils` | |
| 全部参数 DTO | `vectile/dto/v3/*`（含 `toBase32/fromBase32`） | 直接复用，V4 不重复定义 |
| 瓦片范围换算 | `vectile/utils/TileUtils.getTileEnvelope` | |
| JDBC 读取的分页/BBOX SQL | `impl/v3/V3DataReadUtils` | 纯字符串拼接 |
| 阅读参数适配 | `impl/v3/V3LegacyParameterAdapter.toReadParameter` | |

**结论**：V4 要自己写的只有**两件事** —— ① **驱动**（把"逐要素分配瓦片 → 按瓦片聚合 → 编码 → 写出"这条链在一个 JVM 里跑起来），
② **输入读取**（`GIS 文件 / JDBC → GirAdvOneRow`）。编码、分配、写出、归档、参数全部复用。
这让 V4 的工作量从"重写一个切片引擎"降到"重写编排层"，而且**产物天然与 V3 可比**（同一个编码器）。

### 1.2 一个额外发现：mvt-server 目前是把 Spark 跑在 Spring Boot 进程里的

`geoair-mvt-server` 的 `TileGenerationInvoker` 在应用进程内 `SparkSession.builder().getOrCreate()`，
也就是说**管理端自己就是一个 Spark Driver**。改成 V4 之后，管理端可以彻底不加载 Spark 运行时
（少一个 SparkContext、少一份 Spark jar、启动更快、内存占用下降）。

---

## 二、架构决策

### 2.1 新模块

```
geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-v4/     ← 新增（sibling of geoair-static-mvt-spark）
```

- `groupId: cn.geoair.devkit`，`artifactId: geoair-static-mvt-v4`，`version: J8-dev-SNAPSHOT`
- 顶层包：`cn.geoair.map.dynamic.statics.mvt.v4`
- **依赖 `geoair-static-mvt-spark`，但把 Spark / Hadoop / Scala 全部 `exclusion` 掉**：
  这样 V4 用的是 spark 模块里那 44 个无 Spark 依赖的类，而**运行期类路径上没有 Spark**。
  `V3期望.md` 第五节要求的"不动共用代码"因此天然满足 —— V4 **一行都不改** V1/V2/V3 的代码。
- 同时依赖 `geoair-mvt-tools`、`geoair-adv-query`、`geoair-geo-tools`（编码器与查询链路需要）。

### 2.2 隔离机制（这是硬约束，单独说清）

| 隔离项 | 做法 |
| --- | --- |
| **不改 V1/V2/V3 代码** | V4 全部是新文件；spark 模块**零改动**（一个字节都不动） |
| **不改共用类行为** | `MultiLayerMvtEncoderV3`、`VectorTileCommonUtils`、`TileUtils` 等只被**调用**，不被修改 |
| **不共用可变状态** | V4 的聚合容器、溢写文件、进度计数器全是 V4 自己的实例；共用类无静态可变状态 |
| **包名隔离** | V4 用自己的包，类名一律 `V4*` 前缀，不会与 V3 的 `V3*` 混淆 |
| **回归判据** | V1/V2/V3 的代码与产物不受影响 ⇒ 用 `git diff --stat` 可证明 spark 模块无改动；再用同一条任务对比 V3 与 V4 的产物 |

### 2.3 参数模型：V4 参数 = V3 参数 + V4 扩展（结构上保证"一个都不少"）

```java
public class V4TileSliceParameter {
    private MultiLayerTileSliceParameter base;   // V3 的全部参数，原封不动（含 toBase32/fromBase32）
    private V4Options options;                   // V4 新增能力与单机运行时参数
}
```

把 V3 参数**装在里面**而不是"再抄一份"，是刻意设计：V3 以后加字段，V4 自动继承，不会漏。
`V4Options` 只放 V4 真正实现的东西（见 3.2），**不留空壳字段**。

---

## 三、v1 范围

### 3.1 做什么

| # | 内容 |
| --- | --- |
| 1 | 新模块 `geoair-static-mvt-v4`，Spark 排除，运行期无 Spark |
| 2 | `V4VectorTileGenerator`：单 JVM 驱动 —— 校验 → 建表 → 逐图层读 → 逐要素分配瓦片 → 按瓦片聚合（内存 + **溢写**）→ 编码 → 写出 → 元数据 → 归档 |
| 3 | Spark-free 输入层：GeoJSON（FeatureCollection / GeoJSON Lines，含 `.gz`）+ JDBC（`ID_PAGE` / `BBOX`） |
| 4 | 5 种输出全部保留：PostgreSQL / 本地目录 / S3 / MBTiles / PMTiles |
| 5 | 元数据清单与 V3 同结构（字段一致，见 `V3期望` 3.2） |
| 6 | 进度：不依赖 Spark 的要素数/瓦片数进度上报 |
| 7 | 新增 tippecanoe 能力 **4 项**（见 3.2），全部是 V4 侧实现，不动编码器 |
| 8 | 与 V3 的**同口径对拍**验证（同一数据集、同一参数，比瓦片集合/要素数/属性数/字节数） |

### 3.2 本轮实现的新增 tippecanoe 能力（4 项）

| 参数 | 怎么实现 | 落在哪 |
| --- | --- | --- |
| `--preserve-input-order` | 读取时给每行打全局递增序号；编码前按序号稳定排序 | V4 输入层 + 编码前排序 |
| `--reorder` | 编码前按要素在瓦片内的空间序（质心 → 网格编码）重排，让相邻要素在 buffer 里相邻 | V4 编码前排序 |
| `--hilbert` | 同上，但用 Hilbert 曲线索引作为空间序 | V4 编码前排序 |
| `--attribute-type` | 读取时按声明强制属性类型（如 `field=float`） | V4 输入层 |

**为什么先做这 4 项**：它们全都**只依赖 V4 自己**（输入层 + 编码前一步），不需要碰编码器、
不需要碰共用类，与"隔离"这条硬约束完全兼容。其余 7 项都要改编码器内部或需要新的几何算法，
放到 roadmap（见第五节），不在 v1 里假装支持。

### 3.3 v1 不做（明确列出，避免"以为做了"）

| 不做的事 | 原因 |
| --- | --- |
| 全量外排（多路归并 + 分桶溢写到极致） | v1 做"内存 + 顺序溢写 + 单次归并"，够覆盖百万级；更激进的落盘策略等实测瓶颈再上 |
| 分布式执行 | 明确不要 —— 这正是"脱离 Spark"的代价，v1 是单机多线程 |
| 其余 7 项 tippecanoe 参数 | 见第五节 roadmap，逐项写了依赖 |
| 管理端切换默认引擎 | v1 提供开关与两种引擎，默认值等对拍验证之后再定 |

---

## 四、边界决策（我替用户拍板的部分，逐条列明）

> 用户授权"有边界问题按我的想法先确认一版"。以下是我拍的板，都写在明面上，不同意可以推翻。

| # | 决策 | 理由 |
| --- | --- | --- |
| D1 | V4 **另起模块**，而不是在 spark 模块里加 `impl/v4` | 在同一个模块里，产物仍然带着 Spark 依赖，"脱离"名不副实；另起模块才能真正把 Spark 从类路径摘掉，而且 spark 模块零改动，隔离最彻底 |
| D2 | 依赖 spark 模块但 **exclusion 掉 Spark**，而不是把可复用类搬出来另建 core 模块 | 搬类要改 40+ 个文件的位置与模块依赖，风险高、收益只是"目录干净"；exclusion 零风险，且可逆 |
| D3 | V4 参数**包住** V3 参数，而不是平铺复制 | 保证"V3 的能力一个不少"这件事成为结构事实，而不是靠人工对齐清单 |
| D4 | v1 **只实现 4 项**新参数，其余留 roadmap | 宁可 4 项真能跑，也不要 11 项挂名。挂名会让"是否具备 tippecanoe 能力"这件事无法判定 |
| D5 | v1 **不引入新依赖**（不加外部排序库、不加缓存库） | 降低部署复杂度；溢写用 JDK 序列化即可（`GirAdvOneRow` 与 `V3TileFeatureGroup` 都 `implements Serializable`） |
| D6 | 管理端接入用**开关**而非直接替换 | 对拍未完成前不改变现有任务的行为；开关默认值在验证后定 |
| D7 | `-T/--no-tile-stats` 本轮**不实现** | 核实过：`StatisticUtils` 只被 V1/V2 使用，**V3 本来就不写瓦片统计**。V4 与 V3 一致，没有"可关闭的对象"；要支持它等于先新增一项 V3 都没有的能力 |

---

## 五、后续 roadmap（v1 之后）

### 5.1 剩余 7 项 tippecanoe 参数

| 参数 | 卡在哪 | 建议做法 |
| --- | --- | --- |
| `--accumulate-attribute` | 语义是"要素被合并时累加属性"，而合并发生在**编码器内部**（`coalesceBySpatialDensity` → `mergeCell`） | 需要编码器暴露一个"合并回调"或在 V4 侧先做合并；v1 不动编码器 |
| `-B/--base-zoom` | 影响"低层级该不该丢要素"的判定，落在编码器的降级阶梯里 | 与 `MultiLayerMvtEncoderV3.encodeWithTileLimit` 协同，属于改共用类，要先过 `V3期望` 第五节的三条出路 |
| `--minimum-detail` | 同理，需要控制编码器的简化级别下限 | 同上 |
| `--extend-zooms-if-still-dropping` | 需要"切片后判断是否仍在丢、再追加层级"的多轮驱动 | 在 V4 驱动层可以做（V4 主循环自己控制），但涉及层级重跑与输出合并，v1 不做 |
| `--detect-shared-borders` | 拓扑算法：相邻多边形的共享边检测 + 联合简化 | 独立算法模块，工作量最大的一项 |
| `--no-tiny-polygon-reduction` | **V3 本来就没有微小多边形合并**，这个开关没有可关闭的对象 | 要先实现"微小多边形合并"，才有这个反向开关 |
| `-T/--no-tile-stats` | 见 D7 | 要先有瓦片统计 |

### 5.2 运行时能力的补齐

1. **溢写策略升级**：分桶溢写 + 多路归并 + 大瓦片单独处理（v1 是内存 + 顺序 run 归并）
2. **并发编码**：V4 是单机，编码可以多线程并行（每瓦片独立），v1 先单线程保证与 V3 可比，再用实测决定是否并发
3. **内存压测**：给出"多少万要素需要多少堆"的实测曲线，替代"能不能扛住百万级"的猜测

### 5.3 管理端

1. 对拍通过后，把默认引擎切到 V4，并从 `mvtserver-core` 的依赖里摘掉 `geoair-static-mvt-spark`
2. 前端任务页增加"执行引擎"选项（可选）

---

## 六、验证方法

1. **编译与运行隔离**：V4 单模块 `mvn package`；用 `java -cp "v4.jar;lib/*"` 起一个不加载 Spark 的进程跑切片，日志里确认没有 SparkContext 初始化。
2. **同口径对拍**（核心）：poi 数据集（76.3 万点 GeoJSON），参数取历史任务原样，
   `LOCAL_DIRECTORY` 输出，4490 与 3857 各一次；比对：
   - 瓦片集合（z/x/y 是否一致）
   - 每瓦片要素数、属性数（解码 MVT）
   - 字节数差异
3. **输入覆盖**：GeoJSON 文档 / Lines / `.gz`；JDBC 的 `ID_PAGE` 与 `BBOX`。
4. **输出覆盖**：本地目录 + MBTiles + PMTiles（3857 下）；PostgreSQL 视本机是否有可用库。
5. **新参数验证**：4 个新增参数各自开/关对比产物字节（`preserveInputOrder` 改顺序、
   `reorder`/`hilbert` 改顺序且体积变化、`attributeType` 改属性类型）。
6. **V1/V2 未受影响**：`git diff --stat` 证明 spark 模块零改动。

---

## 七、实施记录与完成情况

> 本节在 v1 完成并验证之后回填。

### 7.1 交付物

新模块 `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-v4/`，**16 个 Java 文件、2282 行**：

| 文件 | 行数 | 做什么 |
| --- | --- | --- |
| `V4VectorTileGenerator` | 393 | 驱动：校验 → 建表 → 逐图层读 → 分配瓦片 → 聚合 → 编码写出 → 元数据 → 归档 |
| `group/V4TileGroupStore` | 507 | 内存缓冲 + 提前安全阀 + 溢写 + k 路归并 |
| `input/V4GeoJsonFeatureReader` | 280 | GeoJSON 文档 / Lines / `.gz` 的流式读取 |
| `V4TileWriter` | 196 | 编码 + 落盘（本地目录 / S3 / 归档 / PostgreSQL 批量） |
| `input/V4GeoJsonFeatureMapper` | 146 | Feature → 行对象；`--attribute-type` 类型强制 |
| `group/V4FeatureOrdering` | 146 | `--preserve-input-order` / `--reorder` / `--hilbert` 三种排序 |
| `dto/V4TileSliceParameter` | 121 | 包装 V3 参数 + V4 扩展项，含 Base32 票据 |
| `input/V4JdbcFeatureReader` | 114 | ID 分页 / BBOX 两种 JDBC 读取策略 |
| `V4SliceProgress` | 90 | 不依赖 Spark 的阶段进度与计数 |
| `input/V4InputValidator` | 84 | 输入参数校验（V3 规则的重写，原因见 7.4） |
| `dto/V4Options` | 76 | 新增能力开关 + 单机运行时参数 |
| 其余 5 个（接口/小工具） | 133 | `V4FeatureReader`、`V4RowConsumer`、`V4OrderedRow`、`V4TileConsumer`、`V4FeatureReaderFactory` |

改动面：仓库里只有 **2 处** —— 新增模块目录，以及 `geoair-mvt/pom.xml` 里加一行 `<module>`。
**`geoair-static-mvt-spark` 一个字节都没改**，V1/V2/V3 的行为因此不受影响（`git status` 可证）。

### 7.2 验证结果：与 V3 同口径对拍

方法：从管理端库的任务表取**历史 V3 任务的参数票据**（Base32），只把输出方式改成本地目录，
其余参数（级别、网格、大小上限、gzip、图层配置）原样不动，用 V4 跑一遍，与 V3 的产物逐块比。

**4490 · poi 76.3 万点 · z6–z9**

| 指标 | V3 | V4 |
| --- | --- | --- |
| 瓦片数 | 92 | **92** |
| 瓦片集合 | — | **完全一致**（互相都没有对方没有的块） |
| 总字节 | 37,288,126 | 37,288,375（+0.0007%） |
| 逐块字节差 | — | 最大 313 字节，中位 43.5 字节，相对最大 0.178%（**大量瓦片逐字节相同**） |
| 耗时 | 122.4s | **19.7s** |

**3857 · poi 76.3 万点 · z6–z9**

| 指标 | V3 | V4 |
| --- | --- | --- |
| 瓦片数 | 102 | **102** |
| 瓦片集合 | — | **完全一致** |
| 总字节 | 40,377,668 | 40,377,661（**−7 字节**） |
| 逐块字节差 | — | 最大 235 字节，中位 44.0 字节 |

**解码抽查**（4490 z9 里 V3 最大的三块瓦片）：要素数与属性数**完全相同**，
字节数也完全相同（9783/24、8359/24、8172/24）。说明聚合安全阀削出来的要素集合与 V3 一致 ——
这一点很关键：V4 是在**聚合过程中反复施加**同一个安全阀，V3 是在 `reduceByKey` 里反复施加，
两条路径收敛到同一个集合（证明见 `V4TileGroupStore.capTileIfNeeded` 的注释）。

逐块那几十字节的差异来自**瓦片内要素顺序**（V3 是 reduce 顺序、V4 是输入顺序），
只影响 protobuf 变长整数的字节布局，不影响内容。

### 7.3 新增 4 个 tippecanoe 能力的实测

以 4490 z6–z9 为基准（默认配置 37,288,375 字节 / 92 块）：

| 参数 | 瓦片数 | 字节数 | 相对基准 | 说明 |
| --- | --- | --- | --- | --- |
| 基准（都不开） | 92 | 37,288,375 | — | |
| `--preserve-input-order` | 92 | 35,455,703 | −4.9% | 按输入序号排序 |
| `--reorder` | 92 | 36,230,289 | −2.8% | 按空间网格序 |
| `--hilbert` | 92 | 36,000,067 | −3.5% | 按 Hilbert 曲线序 |
| `--attribute-type type_code=int` | 92 | 37,339,459 | +0.14% | 字符串字段转 int，属性编码方式随之变化 |
| `--attribute-type name=int`（非法） | 92 | 37,286,874 | ≈基准 | 全部转换失败，**保留原值**并告警 763,433 次 |

空间排序让相邻要素在 PBF 里相邻，几何增量编码更省 —— 体积下降方向与 tippecanoe 的
`--reorder` / `--hilbert` 预期一致。属性类型强制则验证了两条路径：转换成功会改变字节，
转换失败不会破坏数据、只累计告警。

### 7.4 实施中发现并处理的三个问题

**① `V3FeatureReaderFactory` 不能复用（原计划误判）。**
最初的判断依据是"没有 `import org.apache.spark`"，据此把它列进了可复用清单。实跑时直接
`NoClassDefFoundError: org/apache/spark/api/java/function/FlatMapFunction` ——
该类本身没 import Spark，但它的**静态字段**会 new 两个实现 Spark 函数接口的读取器，
类加载时就必须能解析那些接口。
**结论：判据要改成"这个类在初始化与执行路径上触达的所有类都必须无 Spark"，不能只看 import。**
处理：V4 自己写了 `V4InputValidator`，规则与 V3 逐条对齐。

**② 聚合安全阀必须提前施加，否则低层级直接 OOM。**
第一版只在编码前削一次，结果 poi z6–z9 在归并阶段 OOM（4GB 堆）——
单个低层级瓦片会在内存里堆到几百万行（实测总分配行数 1221 万，因为一个点会被分配到多块瓦片）。
改成"单个瓦片超过 `图层安全上限 × 2` 就立即削一次"之后：全程 **0 次溢写**、峰值堆降到
1–2GB、耗时 19.7s。

**③ 溢写临时目录不能直接用 `Files.createTempDirectory(prefix)`。**
它依赖 `java.io.tmpdir` 已存在，实测该目录不存在时抛 `NoSuchFileException`。
改成先在 `java.io.tmpdir` 下建固定父目录 `v4-spill`，再在其下建临时子目录。

### 7.5 已验证 / 未验证清单

**已验证**

- 编译：`mvn -pl geoair-static-mvt-v4 install` 通过
- **运行期无 Spark**：依赖树排除 Spark/Hadoop 后，运行时 classpath 141 个 jar，
  其中只有复用模块的 jar 与 `scala-library`（复用的接口签名用到 `scala.Tuple2/Tuple4`），
  没有 spark-core / spark-sql / hadoop
- 输入：GeoJSON 文档模式（76.3 万要素）
- 网格：4490 与 3857 两套
- 输出：本地目录、MBTiles 归档（102 块，元数据完整可读）
- 参数：`--preserve-input-order` / `--reorder` / `--hilbert` / `--attribute-type`
- 与 V3 的同口径对拍（见 7.2）

**未验证（明确列出，不当成已完成）**

| 项 | 状态 |
| --- | --- |
| GeoJSON Lines 模式与 `.gz` 压缩 | 代码已写，**未实测** |
| JDBC 输入（`ID_PAGE` / `BBOX`） | 代码已写，**未实测**（本机无可用空间库） |
| S3 输出、PMTiles 归档、PostgreSQL 输出 | 复用 V3 实现，**未实测** |
| 大要素量压测（千万级） | 未做；只有 76.3 万点的实测数据 |
| 溢写路径本身 | 逻辑已实现，但**本次全程 0 次溢写**，未被真实触发过 |
| 管理端接入 | 未做（见 7.6） |
| V1/V2/V3 回归 | 不需要 —— 它们的代码未被改动 |

### 7.6 管理端接入：本轮不做，只留接口

`geoair-mvt-server` 目前用 `TileGenerationInvoker` 在 Spring Boot 进程内建 `SparkSession` 调 V3。
V4 接入只需把那一处换成 `V4VectorTileGenerator`，**但本轮不改**，原因写在 D6：
对拍只覆盖了 GeoJSON + 本地目录/MBTiles，JDBC、S3、PostgreSQL 三条路径都没实测，
现在切默认引擎风险大于收益。

接入时要做的事（留给下一步）：`TileGenerationInvoker` 增加引擎选择、`mvtserver-core` 依赖里
加上 `geoair-static-mvt-v4`、任务页加一个引擎选项、跑一轮三路径的实测。

### 7.7 下一步优先级

1. **补未验证的三类**：JDBC 输入、S3/PostgreSQL 输出、GeoJSON Lines
2. **触发一次真实溢写**：把阈值调到几千行，验证多 run 归并路径（本次没走到）
3. **千万级压测**：给出"多少要素需要多少堆"的实测曲线
4. 管理端接入与默认引擎切换
5. 剩余 7 项 tippecanoe 参数（第五节 roadmap）

---

## 八、附录：核实过的关键代码位置

| 内容 | 路径（相对 `geoair-framework/`） |
| --- | --- |
| V4 模块 | `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-v4/` |
| 复用的编码器 | `geoair-modules/geoair-geo/geoair-mvt/geoair-static-mvt-spark/.../vectile/impl/v3/MultiLayerMvtEncoderV3.java` |
| 要素→瓦片分配 | `.../vectile/utils/VectorTileCommonUtils.java` |
| 聚合单元 | `.../vectile/impl/v3/V3TileFeatureGroup.java` |
| 输出工厂 | `.../vectile/impl/v3/output/V3TileStoreFactory.java` |
| 归档 | `.../vectile/impl/v3/archive/MbtilesArchiveUtils.java`、`PmtilesUtils.java` |
| V3 驱动（V4 的流程蓝本） | `.../vectile/impl/v3/SparkVectorTileGeneratorV3.java` |
| 调用方接入点 | `geoair-mvt-server/mvtserver-boot/mvtserver-core/.../support/TileGenerationInvoker.java` |
